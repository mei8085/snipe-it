# Snipe-IT 维护记录与附件关联机制分析报告

## 一、整体架构概览

Snipe-IT 系统中，维护记录（Maintenance）与附件的关联并非通过独立的附件表实现，而是**复用了统一的 `action_logs` 操作日志表**作为附件元数据的存储载体，物理文件则存储在磁盘目录中。

核心参与组件：

| 组件 | 路径 | 职责 |
|------|------|------|
| Maintenance 模型 | [Maintenance.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Maintenance.php) | 维护记录数据模型，通过 Trait 获得附件能力 |
| HasUploads Trait | [HasUploads.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Traits/HasUploads.php) | 提供 `uploads()` 关联关系，查询有效附件 |
| Loggable Trait | [Loggable.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Traits/Loggable.php) | 提供 `logUpload()` 方法，记录附件上传日志 |
| Actionlog 模型 | [Actionlog.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Actionlog.php) | 操作日志模型，承载附件元数据（文件名、备注等） |
| MaintenancesController | [MaintenancesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php) | 维护记录的 UI 控制器，处理创建/更新/删除 |
| UploadedFilesController | [UploadedFilesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/UploadedFilesController.php) | 通用文件上传/下载/删除控制器 |
| UploadFileRequest | [UploadFileRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/UploadFileRequest.php) | 文件上传验证与处理 |
| MaintenanceObserver | [MaintenanceObserver.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Observers/MaintenanceObserver.php) | 维护记录生命周期事件监听 |

---

## 二、附件上传流程详解

### 2.1 上传入口

维护记录的附件上传有**两条独立路径**：

**路径 A：创建/编辑维护记录时同步上传**

在 [MaintenancesController::store()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php#L79-L123) 和 [MaintenancesController::update()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php#L163-L190) 中，保存维护记录后调用 `storeUploadedFiles()` 处理附件。

```php
// store() 方法关键步骤
$maintenance->save();
$this->storeUploadedFiles($request, $maintenance);
```

**路径 B：维护记录详情页独立上传**

通过通用文件上传接口，UI 路由 `ui.files.store` 和 API 路由 `api.files.store`，由 [UploadedFilesController::store()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/UploadedFilesController.php#L35-L69) 处理。

### 2.2 文件验证

上传前通过 [MaintenancesController::validateUploadedFiles()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php#L228-L240) 进行验证：

```php
private function validateUploadedFiles(ImageUploadRequest $request): void
{
    if (! $request->hasFile('file')) {
        return;
    }
    $uploadFileRequest = app(UploadFileRequest::class);
    Validator::make(
        array_merge($request->all(), ['file' => $request->file('file')]),
        $uploadFileRequest->rules()
    )->validate();
}
```

[UploadFileRequest::rules()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/UploadFileRequest.php#L30-L37) 定义了验证规则：

```php
public function rules()
{
    $max_file_size = Helper::file_upload_max_size();
    return [
        'file.*' => 'required|mimes:'.config('filesystems.allowed_upload_extensions_for_validator').'|max:'.$max_file_size,
    ];
}
```

允许的扩展名在 [filesystems.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/config/filesystems.php#L126-L159) 中配置，包括：`avif, doc, docx, gif, ico, jpeg, jpg, json, key, lic, mov, mp3, mp4, odp, ods, odt, ogg, pdf, png, rar, rtf, svg, txt, wav, webm, webp, xls, xlsx, xml, zip`。

### 2.3 文件存储

[MaintenancesController::storeUploadedFiles()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php#L195-L223) 处理文件存储：

```php
private function storeUploadedFiles(ImageUploadRequest $request, Maintenance $maintenance): void
{
    $objectType = 'maintenances';
    $storagePath = self::$map_storage_path[$objectType];  // private_uploads/maintenances/

    if (! Storage::exists($storagePath)) {
        Storage::makeDirectory($storagePath, 775);
    }

    $uploadFileRequest = app(UploadFileRequest::class);

    foreach ((array) $request->file('file') as $file) {
        $fileName = $uploadFileRequest->handleFile(
            $storagePath,
            self::$map_file_prefix[$objectType].'-'.$maintenance->id,  // maintenance-{id}
            $file
        );
        $maintenance->logUpload($fileName, $request->input('file_notes'));
    }
}
```

**存储路径映射**定义在 [Controller.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/Controller.php#L66-L98)：

```php
public static $map_storage_path = [
    'maintenances' => 'private_uploads/maintenances/',
];
public static $map_file_prefix = [
    'maintenances' => 'maintenance',
];
```

**文件名生成规则**在 [UploadFileRequest::handleFile()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/UploadFileRequest.php#L43-L63)：

```php
public function handleFile(string $dirname, string $name_prefix, $file): string
{
    $extension = $file->getClientOriginalExtension();
    $file_name = $name_prefix.'-'.str_random(8).'-'.str_slug(basename($file->getClientOriginalName(), '.'.$extension)).'.'.$file->guessExtension();
    // 例如：maintenance-123-aB3dE4fG-invoice-report.pdf

    // SVG 特殊处理：消毒
    if ($file->getMimeType() === 'image/svg+xml') {
        $uploaded_file = $this->handleSVG($file);
    } else {
        $uploaded_file = file_get_contents($file);
    }

    try {
        Storage::put($dirname.$file_name, $uploaded_file);
    } catch (\Exception $e) {
        Log::debug($e);
    }

    return $file_name;
}
```

**⚠️ 严重缺陷：try/catch 吞噬 Storage::put 异常导致孤立 action_logs**

[UploadFileRequest::handleFile()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/UploadFileRequest.php#L56-L60) 中的 try/catch 存在设计缺陷：

```php
try {
    Storage::put($dirname.$file_name, $uploaded_file);
} catch (\Exception $e) {
    Log::debug($e);  // 仅记录调试日志，不向上抛出
}

return $file_name;  // 无论成功失败都返回文件名
```

**问题链路**：
1. `Storage::put()` 因磁盘满、权限不足、S3 连接失败等原因抛出异常
2. 异常被 catch 捕获，仅 `Log::debug($e)` 记录，**不中止流程，不通知上层**
3. `handleFile()` 仍正常返回生成的 `$file_name`
4. 调用方 `storeUploadedFiles()` 拿到文件名后调用 `$maintenance->logUpload()`
5. **action_logs 表成功插入一条 `action_type='uploaded'` 记录，但磁盘上实际不存在该文件**
6. 形成**"孤立 action_logs"**：数据库记录存在但物理文件缺失

**影响**：
- 用户在前端看到"上传成功"提示，但实际文件无法下载/预览
- 前端表格通过 `exists_on_disk` 字段会显示文件不存在，但用户无有效报错信息
- 后台 `debug` 日志级别默认关闭，问题难以排查
- 污染 `action_logs` 数据，需要额外的数据清理

**修复思路**：应移除 try/catch 或在 catch 中重新抛出异常，让上层控制器处理并返回用户可见的错误提示。

**文件系统配置**在 [filesystems.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/config/filesystems.php#L44-L109)，默认使用 `local` disk（即 `storage_path()` 目录），也支持 S3 私有存储。

### 2.4 与维护记录绑定

文件保存后，调用 [Loggable::logUpload()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Traits/Loggable.php#L521-L541) 在 `action_logs` 表创建记录：

```php
public function logUpload($filename, $note)
{
    $log = new Actionlog;
    $log->item_type = static::class;       // App\Models\Maintenance
    $log->item_id = $this->id;              // 维护记录 ID
    $log->created_by = auth()->id();
    $log->note = $note;                     // 用户填写的附件备注
    $log->target_id = null;
    $log->company_id = $this->resolveLoggableCompanyId();
    $log->created_at = date('Y-m-d H:i:s');
    $log->action_date = date('Y-m-d H:i:s');
    $log->filename = $filename;             // 存储的文件名
    $log->logaction('uploaded');            // action_type = 'uploaded'
    return $log;
}
```

---

## 三、数据库关联机制

### 3.1 表结构

**maintenances 表**（维护记录表）：由 [2015_06_26_213716_create_asset_maintenances_table.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/database/migrations/2015_06_26_213716_create_asset_maintenances_table.php) 创建，包含 `id, asset_id, supplier_id, name, start_date, completion_date, notes, cost` 等字段，**没有直接的附件关联字段**。

**action_logs 表**（操作日志表）：由 [2016_09_04_180400_create_actionlog_table.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/database/migrations/2016_09_04_180400_create_actionlog_table.php) 创建。

### 3.2 关联关系（多态关联）

附件与维护记录通过 **多态关联（Polymorphic Relationship）** 绑定，核心字段：

| action_logs 字段 | 值示例 | 含义 |
|------------------|--------|------|
| `item_type` | `App\Models\Maintenance` | 关联的模型类名（多态类型） |
| `item_id` | `42` | 关联的维护记录 ID |
| `action_type` | `uploaded` | 标识这是一条附件上传记录 |
| `filename` | `maintenance-42-aB3dE4fG-invoice.pdf` | 磁盘上的文件名 |
| `note` | `维修发票` | 附件备注 |

### 3.3 查询有效附件

[HasUploads::uploads()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Traits/HasUploads.php#L9-L22) 定义了查询有效附件的逻辑：

```php
public function uploads()
{
    return $this->hasMany(Actionlog::class, 'item_id')
        ->where('item_type', self::class)
        ->where('action_type', '=', 'uploaded')
        ->whereNotNull('filename')
        ->whereNotIn('filename', function ($query) {
            // 排除已被标记删除的文件
            $query->select('filename')
                ->from('action_logs')
                ->where('item_type', '=', self::class)
                ->where('action_type', '=', 'upload deleted')
                ->where('item_id', $this->id);
        });
}
```

**关键逻辑**：并非通过物理删除 action_logs 记录来删除附件，而是通过新增一条 `action_type='upload deleted'` 的记录来做**软标记**，查询时通过 `whereNotIn` 子查询过滤掉已删除的文件名。

---

## 四、删除维护记录时的附件处理

### 4.1 当前实现分析

**UI 控制器删除**：[MaintenancesController::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php#L283-L293)

```php
public function destroy(Maintenance $maintenance): RedirectResponse
{
    $this->authorize('update', Asset::class);
    $this->authorize('update', $maintenance->asset);
    $maintenance->delete();  // 仅删除维护记录本身
    return redirect()->route('maintenances.index')
        ->with('success', trans('admin/maintenances/message.delete.success'));
}
```

**API 控制器删除**：[Api\MaintenancesController::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/Api/MaintenancesController.php#L306-L317)

```php
public function destroy($maintenanceId): JsonResponse|array
{
    $this->authorize('update', Asset::class);
    $maintenance = Maintenance::findOrFail($maintenanceId);
    $maintenance->delete();  // 仅删除维护记录本身
    return response()->json(Helper::formatStandardApiResponse('success', $maintenance, ...));
}
```

**Observer 删除事件**：[MaintenanceObserver::deleting()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Observers/MaintenanceObserver.php#L103-L114)

```php
public function deleting(Maintenance $maintenance)
{
    $logAction = new Actionlog;
    $logAction->item_type = Maintenance::class;
    $logAction->item_id = $maintenance->id;
    $logAction->target_type = Asset::class;
    $logAction->target_id = $maintenance->asset_id;
    $logAction->created_at = date('Y-m-d H:i:s');
    $logAction->action_date = date('Y-m-d H:i:s');
    $logAction->created_by = auth()->id();
    $logAction->logaction('delete');  // 仅记录删除日志，未处理附件
}
```

### 4.2 对比：其他模型的正确实现

以 [ConsumableObserver::deleting()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Observers/ConsumableObserver.php#L65-L95) 为参考，它正确地处理了附件清理：

```php
public function deleting(Consumable $consumable)
{
    $consumable->users()->detach();
    $uploads = $consumable->uploads;

    foreach ($uploads as $file) {
        try {
            Storage::delete('private_uploads/consumables/'.$file->filename);  // 删除物理文件
            $file->delete();  // 删除日志记录
        } catch (\Exception $e) {
            Log::info($e);
        }
    }
    // ...
}
```

### 4.3 重要发现：维护记录删除存在附件残留问题

**结论：当前 MaintenanceObserver 的 `deleting()` 方法存在缺失——删除维护记录时不会级联清理关联的附件文件和日志记录。**

影响：
1. **磁盘垃圾文件**：`private_uploads/maintenances/` 目录下会残留已删除维护记录的附件
2. **孤立日志记录**：`action_logs` 表中会残留 `item_type=Maintenance` 且 `item_id` 指向已删除记录的上传日志
3. **软删除加剧问题**：Maintenance 模型使用了 `SoftDeletes` trait，即使后续强制删除（forceDelete）也不会触发附件清理

---

## 五、单个附件的删除机制

通过 [UploadedFilesController::destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/UploadedFilesController.php#L127-L157) 可单独删除某个附件：

```php
public function destroy($object_type, $id, $file_id): RedirectResponse
{
    $object = self::$map_object_type[$object_type]::withTrashed()->find($id);
    $this->authorize('files', $object);

    $log = Actionlog::where('id', $file_id)
        ->where('item_type', self::$map_object_type[$object_type])
        ->where('item_id', $object->id)->first();

    if ($log) {
        // 1. 删除物理文件
        if (Storage::exists(self::$map_storage_path[$object_type].$log->filename)) {
            Storage::delete(self::$map_storage_path[$object_type].$log->filename);
        }
        // 2. 记录一条 "upload deleted" 标记（而非删除原日志）
        if ($log->logUploadDelete($object, $log->filename)) {
            return redirect()->back()->withFragment('files')->with('success', ...);
        }
    }
}
```

其中 [Actionlog::logUploadDelete()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Actionlog.php#L567-L579) 的实现：

```php
public function logUploadDelete($object, $filename)
{
    $log = new Actionlog;
    $log->item_type = $object instanceof SnipeModel ? get_class($object) : $object;
    $log->item_id = $object->id;
    $log->created_by = auth()->id();
    $log->target_id = null;
    $log->filename = $filename;
    $log->created_at = date('Y-m-d H:i:s');
    $log->logaction('upload deleted');  // 新增一条删除标记，原 uploaded 记录保留
    return $log;
}
```

---

## 六、完整流程图

### 6.1 附件上传与绑定流程

```
用户提交表单（带 file[] 字段）
    │
    ▼
MaintenancesController::store()
    ├─> validateUploadedFiles() 验证文件类型/大小
    ├─> $maintenance->save() 保存维护记录
    │       └─> MaintenanceObserver::created() 记录创建日志
    └─> storeUploadedFiles()
            ├─> 确保目录 private_uploads/maintenances/ 存在
            └─> 遍历每个文件：
                    ├─> UploadFileRequest::handleFile()
                    │       ├─> 生成文件名：maintenance-{id}-{rand8}-{原文件名}.{ext}
                    │       ├─> SVG 消毒（如适用）
                    │       └─> Storage::put() 写入磁盘
                    └─> $maintenance->logUpload()
                            └─> action_logs 表插入记录：
                                item_type = 'App\Models\Maintenance'
                                item_id   = {维护记录ID}
                                action_type = 'uploaded'
                                filename  = {存储文件名}
                                note      = {附件备注}
```

### 6.2 维护记录删除流程（当前实现）

```
用户点击删除
    │
    ▼
MaintenancesController::destroy()
    ├─> 权限检查
    └─> $maintenance->delete() 【软删除】
            └─> MaintenanceObserver::deleting()
                    └─> action_logs 插入一条 action_type='delete' 的记录
                    └─> ⚠️ 未处理：附件物理文件未删除
                    └─> ⚠️ 未处理：action_logs 中的 uploaded 记录未标记删除
```

### 6.3 维护记录删除流程（期望实现，参考 ConsumableObserver）

```
用户点击删除
    │
    ▼
MaintenanceObserver::deleting() 【期望补充】
    ├─> $maintenance->uploads 获取所有有效附件
    └─> 遍历每个附件：
            ├─> Storage::delete('private_uploads/maintenances/'.$file->filename) 删除物理文件
            └─> $file->logUploadDelete() 或 $file->delete() 处理日志记录
    └─> 继续记录删除日志...
```

---

## 九、关键代码索引

| 功能 | 文件 | 行号 |
|------|------|------|
| 维护记录模型定义 | [Maintenance.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Maintenance.php) | L24-L251 |
| 附件多态关联查询 | [HasUploads.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Traits/HasUploads.php) | L9-L22 |
| 附件上传日志记录 | [Loggable.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Traits/Loggable.php) | L521-L541 |
| UI 控制器创建+上传 | [MaintenancesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php) | L79-L123 |
| 文件存储私有方法 | [MaintenancesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/MaintenancesController.php) | L195-L223 |
| 存储路径映射 | [Controller.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/Controller.php) | L66-L98 |
| 文件验证与保存（含 try/catch 缺陷） | [UploadFileRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/UploadFileRequest.php) | L30-L63 |
| 主图上传处理（handleImages） | [ImageUploadRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/ImageUploadRequest.php) | L73-L154 |
| 通用文件上传接口 | [UploadedFilesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/UploadedFilesController.php) | L35-L69 |
| API 文件列表接口 | [Api/UploadedFilesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/Api/UploadedFilesController.php) | L30-L77 |
| 通用文件删除接口 | [UploadedFilesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/UploadedFilesController.php) | L127-L157 |
| 附件数据转换器 | [UploadedFilesTransformer.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Transformers/UploadedFilesTransformer.php) | L25-L70 |
| 附件表格列布局 | [UploadedFilesPresenter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Presenters/UploadedFilesPresenter.php) | L15-L103 |
| 附件表格组件 | [blade/table/files.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/resources/views/blade/table/files.blade.php) | L14-L33 |
| 维护记录详情页 | [maintenances/view.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/resources/views/maintenances/view.blade.php) | L191-L193 |
| 文件 URL/路径生成 | [Actionlog.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Actionlog.php) | L581-L645 |
| 附件删除标记记录 | [Actionlog.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Models/Actionlog.php) | L567-L579 |
| image 列迁移 | [2025_08_06_192954_add_image_to_maintenances.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/database/migrations/2025_08_06_192954_add_image_to_maintenances.php) | L14-L18 |
| 维护记录删除事件（有缺失） | [MaintenanceObserver.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Observers/MaintenanceObserver.php) | L103-L114 |
| 维护记录恢复事件 | [MaintenanceObserver.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Observers/MaintenanceObserver.php) | L116-L129 |
| 耗材删除事件（参考实现） | [ConsumableObserver.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Observers/ConsumableObserver.php) | L65-L95 |
| 文件系统配置 | [filesystems.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/config/filesystems.php) | L1-L186 |
| action_logs 表迁移 | [2016_09_04_180400_create_actionlog_table.php](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/database/migrations/2016_09_04_180400_create_actionlog_table.php) | L13-L38 |

---

## 十、总结

### 核心机制总结

1. **关联设计**：维护记录与通用附件通过 `action_logs` 表的多态关联（`item_type` + `item_id`）实现，无独立附件表；主图则直接存储在 `maintenances.image` 列。
2. **文件存储**：通用附件物理文件存于 `private_uploads/maintenances/`（私有，需权限），命名格式为 `maintenance-{id}-{rand8}-{slug}.{ext}`；主图存于 `public/uploads/maintenances/`（公开访问）。
3. **软删除机制**：单个附件的"删除"并非物理删除日志记录，而是新增 `action_type='upload deleted'` 的标记记录，查询时通过 `whereNotIn` 子查询过滤。
4. **读回链路**：Blade → `<x-table.files>` 组件 → API `/api/v1/files/maintenances/{id}` → `$maintenance->uploads()` → `UploadedFilesTransformer` → 前端表格展示，全程支持 `withTrashed()` 查询已软删除记录的附件。

### 四个细节问题的分析结论

5. **⚠️ handleFile() 异常吞噬导致孤立 action_logs**：[UploadFileRequest::handleFile()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Requests/UploadFileRequest.php#L56-L60) 中 try/catch 吞噬 `Storage::put()` 异常，仅 `Log::debug()` 不向上抛出。当 `Storage::put()` 因磁盘满、权限不足等原因失败时，`handleFile()` 仍返回正常文件名，调用方接着调用 `logUpload()` 在 `action_logs` 表插入记录，形成**"数据库有记录但磁盘无文件"的孤立 action_logs**。用户看到上传成功但实际无法下载，问题隐蔽难以排查。

6. **⚠️ 附件读回链路的 withTrashed 设计**：[Api\UploadedFilesController::index()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/Api/UploadedFilesController.php#L276-L279) 和 [destroy()](file:///d:/fz/0601-1/solo-dogfeeding/code/78-snipe-it/app/Http/Controllers/UploadedFilesController.php#L291) 都使用 `withTrashed()` 查询所有者模型，意味着**即使维护记录已被软删除，管理员仍可直接通过 URL 访问其详情页并管理附件**。这是有意的设计——软删除只是隐藏记录，数据仍完整保留便于恢复。

7. **⚠️ image 列与 uploads 附件的本质差异**：`image` 列是维护记录的**属性字段**（类似 `name`、`cost`），存储单张主图到公开目录，更新时直接覆盖旧值，变更由 `updating` 事件记录到 `action_type='update'` 日志；`uploads` 是**独立的多态关联对象**，支持多文件、多类型，存储到私有目录，每个上传/删除操作单独记录 `action_type='uploaded'/'upload deleted'` 日志。`handleImages()` 不写 `action_logs` 是因为它属于属性更新，而非附件操作。另外，API 端 `store()` 只调 `handleImages()` 不调 `storeUploadedFiles()`，导致通过 API 创建维护记录时**无法同时上传多附件**。

8. **⚠️ SoftDeletes 三态语义断层**：Maintenance 的三种状态（正常、软删除、强制删除）对 `action_logs` 和物理文件的影响完全不同：
   - 正常→软删除：仅主表 `deleted_at` 赋值，`action_logs` 和磁盘文件**完全不动**
   - 软删除→恢复：仅主表 `deleted_at` 清空，无需处理附件
   - 软删除→强制删除：主表记录物理删除，`action_logs` 和磁盘文件**完全不动**，成为永久孤儿数据
   
   再加上 Actionlog 自身的 `deleted_at` 字段和 `upload deleted` 标记记录，系统中并存**三层"删除"语义**，造成极高的理解和维护成本。

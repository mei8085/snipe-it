# Snipe-IT 组织层级权限补充分析

## 1. 地点（Location）和部门（Department）的权限约束作用

### 1.1 Location 模型的权限约束

Location 模型本身的权限策略非常简洁，继承自 `SnipePermissionsPolicy`：

```php
// app/Policies/LocationPolicy.php:7-17
class LocationPolicy extends SnipePermissionsPolicy
{
    protected function columnName()
    {
        return 'locations';
    }
}
```

**Location 的实际权限约束体现在以下几个层面：**

#### 1.1.1 查询过滤层面（CompanyableScope）

Location 的公司范围过滤是**可选**的，受 `scope_locations_fmcs` 配置控制：

```php
// app/Models/Traits/CompanyableTrait.php:15-22
if (__CLASS__ != 'App\Models\Location') {
    static::addGlobalScope(new CompanyableScope);
} else {
    if (Setting::getSettings()?->scope_locations_fmcs == 1) {
        static::addGlobalScope(new CompanyableScope);
    }
}
```

这意味着：
- 当 `scope_locations_fmcs = 0` 时，用户可以看到所有公司的地点
- 当 `scope_locations_fmcs = 1` 时，用户只能看到自己公司的地点

#### 1.1.2 数据验证层面（fmcs_location 规则）

Asset 模型的验证规则中，`location_id` 和 `rtd_location_id` 必须通过 `fmcs_location` 验证：

```php
// app/Providers/ValidationServiceProvider.php:352-365
Validator::extend('fmcs_location', function ($attribute, $value, $parameters, $validator) {
    $settings = Setting::getSettings();
    if ($settings->full_multiple_companies_support == '1' && $settings->scope_locations_fmcs == '1') {
        $company_id = array_get($validator->getData(), 'company_id');
        $location = Location::find($value);
        if (($location) && ($company_id != $location->company_id)) {
            return false;  // 位置必须属于同一公司
        }
    }
    return true;
});
```

#### 1.1.3 业务逻辑层面（资产按位置过滤）

```php
// app/Models/Asset.php:1498-1529
public function scopeAssetsByLocation($query, $location)
{
    return $query->where(function ($query) use ($location) {
        $query->whereHas('assignedTo', function ($query) use ($location) {
            $query->where([['users.location_id', '=', $location->id], ...])
                  ->orWhere([['locations.id', '=', $location->id], ...])
                  ->orWhere([['assets.rtd_location_id', '=', $location->id], ...]);
        })->orWhere(function ($query) use ($location) {
            $query->where('assets.rtd_location_id', '=', $location->id)
                  ->whereNull('assets.assigned_to');
        });
    });
}
```

**Location 权限约束总结**：
- Location 本身没有基于位置的权限继承
- Location 主要作为资产的属性参与权限验证
- 只有当两个 FMCS 开关都启用时，Location 才会参与公司级隔离

### 1.2 Department 模型的权限约束

```php
// app/Policies/DepartmentPolicy.php:7-17
class DepartmentPolicy extends SnipePermissionsPolicy
{
    protected function columnName()
    {
        return 'departments';
    }
}
```

#### 1.2.1 部门的公司级隔离

Department 自动应用 `CompanyableScope`，永远按公司隔离（无可选开关）。

#### 1.2.2 部门的唯一约束

```php
// app/Models/Department.php:47
'name' => 'required|max:255|is_unique_across_company_and_location:departments,name'
```

部门名称在同一公司和地点下必须唯一。

#### 1.2.3 业务逻辑层面（按部门过滤已分配资产）

```php
// app/Models/Asset.php:1946-1959
public function scopeCheckedOutToTargetInDepartment($query, $search)
{
    return $query->leftJoin('users as assets_dept_users', function ($leftJoin) {
        $leftJoin->on('assets_dept_users.id', '=', 'assets.assigned_to')
                 ->where('assets.assigned_type', '=', User::class);
    })->where(function ($query) use ($search) {
        $query->whereIn('assets_dept_users.department_id', $search);
    });
}
```

**Department 权限约束总结**：
- Department 永远按公司隔离
- Department 主要用于用户分类和资产分配统计
- 没有基于部门的权限继承或可见范围限制

### 1.3 地点和部门的定位

| 维度 | 权限约束强度 | 主要作用 | 是否参与数据隔离 |
|------|-------------|----------|----------------|
| **Company** | 强 | 顶级权限边界，核心隔离单元 | 是（核心） |
| **Location** | 中（可选） | 物理位置管理，资产实际位置 | 可选（受 scope_locations_fmcs 控制） |
| **Department** | 中 | 组织架构分类，用户部门归属 | 是（但仅用于自身隔离） |

**重要结论**：在 Snipe-IT 的权限体系中，**Company 是唯一的权限隔离边界**。Location 和 Department 主要作为数据属性和过滤条件，不构成独立的权限边界。

---

## 2. 不同角色在公司/地点/部门组合下的可见范围判定流程

### 2.1 角色定义

| 角色 | 标识条件 | 说明 |
|------|---------|------|
| **超级管理员 (SuperUser)** | `$user->isSuperUser()` | 通常是系统第一个用户，`permissions` 字段包含 `superuser` |
| **管理员 (Admin)** | `$user->hasAccess('admin')` | 拥有 admin 权限，但可能有公司归属 |
| **普通用户 (User)** | 不满足以上条件 | 只有被授权的特定权限 |

### 2.2 可见范围判定总流程

```
用户发起查询
    │
    ▼
是否有 CompanyableTrait? ──否──▶ 无公司隔离
    │是
    ▼
FMCS 是否启用? ──否──▶ 返回所有数据
    │是
    ▼
用户是 SuperUser? ──是──▶ 返回所有数据
    │否
    ▼
用户 company_id 是否为 null? ──是──▶ 返回所有数据（无公司归属的管理员）
    │否
    ▼
自动注入 WHERE company_id = {user.company_id}
    │
    ▼
对查询结果应用权限策略检查
```

### 2.3 各类用户的可见范围详解

#### 2.3.1 超级管理员 (SuperUser)

```php
// app/Models/Company.php:289-297
if (! self::isFullMultipleCompanySupportEnabled() || 
    (Auth::hasUser() && auth()->user()->isSuperUser())) {
    return $query;  // 不过滤
}
```

**可见范围**：
- ✅ 所有公司的资产、用户、地点、部门
- ✅ 不受任何公司边界限制
- ✅ 可以跨公司移动资产

**判定代码位置**：
- 查询过滤：`app/Models/Company.php:292`
- 权限策略：`app/Models/Company.php:188-193`

#### 2.3.2 管理员 (Admin) - 无公司归属 (`company_id = null`)

**可见范围**：
- ✅ 所有公司的资产、用户、地点、部门
- ✅ 不受公司边界限制
- ✅ 可以跨公司移动资产（因为 `getIdForCurrentUser` 返回输入值）

```php
// app/Models/Company.php:138-141
if ($current_user->company_id != null) {
    return $current_user->company_id;
} else {
    return null;  // 无公司归属的管理员可以指定任意公司
}
```

**判定代码位置**：
- 查询过滤：`app/Models/Company.php:290`（FMCS 启用时，`company_id = null` 不触发过滤）
- 公司 ID 处理：`app/Models/Company.php:138-141`

#### 2.3.3 管理员 (Admin) - 有公司归属 (`company_id = C1`)

**可见范围**：
- ✅ 仅公司 C1 的资产、用户、地点、部门
- ❌ 无法看到或操作其他公司的数据
- ❌ 无法跨公司移动资产（`getIdForCurrentUser` 强制返回 C1）

```php
// app/Models/Company.php:138-139
if ($current_user->company_id != null) {
    return $current_user->company_id;  // 强制使用自己的公司
}
```

**判定代码位置**：
- 查询过滤：`app/Models/Company.php:324`（注入 `WHERE company_id = C1`）
- 公司 ID 处理：`app/Models/Company.php:138-139`

#### 2.3.4 普通用户 - 有公司归属 (`company_id = C1`)

**可见范围**：
- ✅ 仅公司 C1 的数据（受查询 Scope 过滤）
- ✅ 只能操作被授权的功能（如查看自己的资产）
- ❌ 无法编辑资产
- ❌ 无法跨公司操作

**权限策略检查**：
```php
// app/Policies/SnipePermissionsPolicy.php:48-62
if (! Company::isCurrentUserHasAccess($item)) {
    return false;  // 即使绕过查询过滤，策略层也会拦截
}
```

### 2.4 地点和部门在可见范围中的角色

**关键点**：Location 和 Department **不直接影响可见范围**，它们只是：

1. **过滤参数**：用户可以通过 `location_id` 或 `department_id` 过滤查询结果
   ```php
   // app/Http/Controllers/Api/AssetsController.php:333-335
   if ($request->filled('location_id')) {
       $assets->where('assets.location_id', '=', $request->input('location_id'));
   }
   ```

2. **数据属性**：资产的属性，用于描述资产的物理位置和分配用户的部门

3. **验证规则**：确保资产的位置与其公司归属一致（当 `scope_locations_fmcs = 1` 时）

### 2.5 可见范围判定矩阵

| 角色 | FMCS | 用户 company_id | 可见范围 | 能否跨公司移动 |
|------|------|----------------|---------|--------------|
| SuperUser | 是/否 | 任意 | 所有公司 | ✅ 是 |
| Admin | 是 | null | 所有公司 | ✅ 是 |
| Admin | 是 | C1 | 仅 C1 | ❌ 否 |
| Admin | 否 | 任意 | 所有公司 | -（无FMCS概念） |
| 普通用户 | 是 | C1 | 仅 C1（且仅授权功能） | ❌ 否 |
| 普通用户 | 否 | 任意 | 所有数据（但仅授权功能） | -（无FMCS概念） |

---

## 3. 跨公司移动资产时的字段一致性校验分析

### 3.1 资产跨公司移动的触发场景

1. **单个资产更新（API）**：`PUT /api/v1/hardware/{id}`
2. **单个资产更新（Web）**：`POST /hardware/{id}/edit`
3. **批量资产更新（Web）**：`POST /hardware/bulkedit`

### 3.2 单个资产更新（API）的校验流程

```php
// app/Http/Controllers/Api/AssetsController.php:759-876
public function update(UpdateAssetRequest $request, Asset $asset)
{
    $asset->fill($request->validated());
    
    // 1. 公司 ID 处理（第766-768行）
    if ($request->has('company_id')) {
        $asset->company_id = Company::getIdForCurrentUser($request->validated()['company_id']);
    }
    
    // 2. 位置处理（第769-771行）
    if ($request->has('rtd_location_id') && ! $request->has('location_id')) {
        $asset->location_id = $request->validated()['rtd_location_id'];
    }
    
    // 3. 借出时的公司一致性检查（第819-823行）
    if ($requestedCheckout) {
        $companyMismatchResponse = $this->checkoutCompanyMismatchResponse($asset, $target);
        if ($companyMismatchResponse) {
            return $companyMismatchResponse;
        }
    }
    
    // 4. 保存（第826-856行）
    $updated = DB::transaction(function () use ($asset, $request, $target, $requestedCheckout) {
        if (! $asset->save()) {
            return false;
        }
        // ... checkout 逻辑
    });
}
```

#### 3.2.1 借出时的公司一致性检查

```php
// app/Http/Controllers/Api/AssetsController.php:895-906
private function checkoutCompanyMismatchResponse(Asset $asset, User|Asset|Location $target): ?JsonResponse
{
    if ((Setting::getSettings()->full_multiple_companies_support == '1')
        && (! is_null($asset->company_id))
        && (! is_null($target->company_id))
        && ((int) $asset->company_id !== (int) $target->company_id)
    ) {
        return response()->json(Helper::formatStandardApiResponse('error', null, trans('general.error_user_company')));
    }
    return null;
}
```

**检查点**：
- ✅ 资产公司与借出目标公司一致
- ✅ 仅在借出时检查
- ❌ 更新公司字段时不检查现有分配目标

#### 3.2.2 公司 ID 处理逻辑

```php
// app/Models/Company.php:127-145
public static function getIdForCurrentUser($unescaped_input)
{
    if (! self::isFullMultipleCompanySupportEnabled()) {
        return self::getIdFromInput($unescaped_input);
    }
    
    $current_user = auth()->user();
    
    if ($current_user->isSuperUser()) {
        return self::getIdFromInput($unescaped_input);  // 超级用户可自由指定
    } else {
        if ($current_user->company_id != null) {
            return $current_user->company_id;  // 非超级用户强制使用自己的公司
        } else {
            return null;
        }
    }
}
```

### 3.3 单个资产更新（Web）的校验流程

```php
// app/Http/Controllers/Assets/AssetsController.php:440-525
public function update(Request $request, Asset $asset)
{
    // ...
    $asset->company_id = Company::getIdForCurrentUser($request->input('company_id'));  // 第463行
    // ...
    if ($asset->save()) {  // 第519行
        return Helper::getRedirectOption(...)
            ->with('success', trans('admin/hardware/message.update.success'));
    }
}
```

**校验点分析**：
- ✅ 公司 ID 通过 `getIdForCurrentUser` 处理（权限边界）
- ✅ 模型层验证：`location_id` 和 `rtd_location_id` 通过 `fmcs_location` 规则
- ❌ 没有检查资产已分配目标的公司一致性
- ❌ 没有检查位置与新公司的一致性

### 3.4 批量资产更新的校验流程

```php
// app/Http/Controllers/Assets/BulkAssetsController.php:374-379
if ($request->filled('company_id')) {
    $this->update_array['company_id'] = Company::getIdForCurrentUser($request->input('company_id'));
    if ($request->input('company_id') == 'clear') {
        $this->update_array['company_id'] = null;
    }
}

// 第429-443行：位置更新
if ($request->filled('rtd_location_id')) {
    if (($request->filled('update_real_loc')) && (($request->input('update_real_loc')) == '0')) {
        $this->update_array['rtd_location_id'] = $request->input('rtd_location_id');
    }
    // ...
}

// 第521-532行：保存
if (! $asset->update($this->update_array)) {
    // 处理错误
}
```

### 3.5 字段一致性校验现状总结

| 字段 | 创建时校验 | 单个更新时校验 | 批量更新时校验 | 说明 |
|------|-----------|---------------|---------------|------|
| **company_id** | ✅ `getIdForCurrentUser` | ✅ `getIdForCurrentUser` | ✅ `getIdForCurrentUser` | 按用户权限强制 |
| **location_id** | ✅ `fmcs_location` 规则 | ✅ `fmcs_location` 规则 | ❌ 仅更新值，无一致性检查 | 批量更新时可能出现公司不匹配 |
| **rtd_location_id** | ✅ `fmcs_location` 规则 | ✅ `fmcs_location` 规则 | ❌ 仅更新值，无一致性检查 | 批量更新时可能出现公司不匹配 |
| **assigned_to（借出时）** | ✅ `checkoutCompanyMismatchResponse` | ✅ `checkoutCompanyMismatchResponse` | - | 仅借出时检查 |
| **assigned_to（已分配）** | - | ❌ 无检查 | ❌ 无检查 | 更新公司时不验证现有分配 |

### 3.6 潜在风险场景

#### 风险场景 1：批量更新公司但不更新位置
```
资产 A: company_id=1, location_id=100 (位置100属于公司1)
批量更新: company_id=2, 不更新 location_id
结果: 资产 company_id=2, location_id=100 (位置100仍属于公司1)
状态: 数据不一致 ❌
```

#### 风险场景 2：更新已分配资产的公司
```
资产 A: company_id=1, assigned_to=用户U1 (U1属于公司1)
更新: company_id=2
结果: 资产 company_id=2, assigned_to=U1 (U1仍属于公司1)
状态: 数据不一致 ❌
```

#### 风险场景 3：借出目标查找绕过全局 Scope
```php
// app/Http/Controllers/Api/AssetsController.php:880-881
if ($request->filled('assigned_user')) {
    return User::withoutGlobalScopes()->find($request->input('assigned_user'));
}
```
使用 `withoutGlobalScopes()` 可以找到其他公司的用户，但后续有 `checkoutCompanyMismatchResponse` 检查，所以这个风险是可控的。

### 3.7 优化建议

#### 建议 1：批量更新时增加公司-位置一致性检查
```php
// 在 BulkAssetsController::post() 的更新循环中添加
if (isset($this->update_array['company_id']) && $settings->scope_locations_fmcs == '1') {
    $new_company_id = $this->update_array['company_id'];
    
    // 检查现有 location_id
    if ($asset->location_id) {
        $location = Location::find($asset->location_id);
        if ($location && $location->company_id != $new_company_id) {
            // 报错或自动清除位置
            $error_array[] = "资产 {$asset->id} 的位置不属于新公司";
            continue;
        }
    }
    
    // 检查现有 rtd_location_id
    if ($asset->rtd_location_id) {
        $location = Location::find($asset->rtd_location_id);
        if ($location && $location->company_id != $new_company_id) {
            $error_array[] = "资产 {$asset->id} 的默认位置不属于新公司";
            continue;
        }
    }
}
```

#### 建议 2：更新公司时检查已分配目标
```php
// 在资产更新时添加
if (isset($this->update_array['company_id']) && $asset->assigned_to) {
    $target = $asset->assignedTo;
    if ($target && $target->company_id != $this->update_array['company_id']) {
        // 报错或自动解除分配
        $asset->assigned_to = null;
        $asset->assigned_type = null;
    }
}
```

#### 建议 3：Web 端单个更新增加一致性检查
在 `AssetsController::update()` 中增加类似 API 端的检查逻辑。

---

## 4. 总结

### 4.1 核心结论

1. **Company 是唯一的权限隔离边界**：Location 和 Department 不构成独立的权限边界，仅作为数据属性和过滤条件。

2. **三层防护机制**：
   - 查询层：`CompanyableScope` 自动过滤
   - 策略层：`SnipePermissionsPolicy::before()` 拦截跨公司访问
   - 验证层：`fmcs_location` 等规则确保写入一致性

3. **超级用户 ≠ 无公司归属的管理员**：两者都能看到所有数据，但实现路径不同（SuperUser 跳过 Scope，无公司归属的管理员不触发过滤条件）。

4. **跨公司移动资产存在数据一致性风险**：批量更新和 Web 端单个更新时，没有检查位置和分配目标与新公司的一致性。

### 4.2 关键代码位置速查

| 功能 | 文件位置 | 行号 |
|------|---------|------|
| 公司访问判断 | `app/Models/Company.php` | 153-196 |
| 公司 ID 处理 | `app/Models/Company.php` | 127-145 |
| 查询 Scope 应用 | `app/Models/Company.php` | 289-328 |
| 权限策略前置检查 | `app/Policies/SnipePermissionsPolicy.php` | 38-73 |
| 借出公司一致性检查 | `app/Http/Controllers/Api/AssetsController.php` | 895-906 |
| fmcs_location 验证规则 | `app/Providers/ValidationServiceProvider.php` | 352-365 |
| 资产按位置过滤 | `app/Models/Asset.php` | 1498-1529 |
| 资产按部门过滤 | `app/Models/Asset.php` | 1946-1959 |

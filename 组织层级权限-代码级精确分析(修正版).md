# Snipe-IT 组织层级权限 - 代码级精确分析（修正版）

> **说明**：本文档基于 Laravel 12 + Snipe-IT 代码逐行核对，结合测试用例验证，修正了之前对空值查询语义和无公司归属管理员行为的理解偏差。

---

## 1. 关键代码核对结论

### 1.1 Laravel Query Builder NULL 值处理

**核心发现**：Laravel 的 `where('column', '=', null)` 会被自动转换为 `whereNull('column')`。

**证据**：
- Laravel 12 的 Query Builder 在编译 SQL 时，会检测 `$value === null` 并自动使用 `IS NULL` 语义
- 这是 Laravel 框架的内置行为，旨在避免 SQL 中的 `NULL = NULL` 问题

**验证**：
```php
// 代码写法
Asset::query()->where('company_id', '=', null)->get();

// 实际生成的 SQL
SELECT * FROM assets WHERE assets.company_id IS NULL
```

这意味着之前关于 "无公司归属管理员查询空结果" 的结论是**错误的**。

---

### 1.2 scopeCompanyables 精确逻辑

```php
// app/Models/Company.php:289-297
public static function scopeCompanyables($query, $column = 'company_id', $table_name = null)
{
    // 跳过 Scope 的三个条件（OR 关系）
    if (! self::isFullMultipleCompanySupportEnabled()       // 1. FMCS 未启用
        || (Auth::hasUser() && auth()->user()->isSuperUser())  // 2. 已登录且是 SuperUser
        || (! Auth::hasUser())) {                              // 3. 未登录（CLI）
        return $query;  // 不做任何过滤
    } else {
        return self::scopeCompanyablesDirectly($query, $column, $table_name);
    }
}
```

**关键点**：
- 无公司归属的管理员 **不满足** 上述任何跳过条件（不是 SuperUser，且已登录）
- 因此会进入 `scopeCompanyablesDirectly()`

---

### 1.3 scopeCompanyablesDirectly 精确逻辑

```php
// app/Models/Company.php:305-328
private static function scopeCompanyablesDirectly($query, $column = 'company_id', $table_name = null)
{
    $company_id = null;
    if (Auth::hasUser()) {
        $company_id = auth()->user()->company_id;  // 获取当前用户的 company_id
    }

    // 对 companies 表自身的特殊处理
    if ($query->getModel()->getTable() == 'companies') {
        return $query->where('companies.id', '=', $company_id);
    }

    // 通用：注入 WHERE 条件
    if (Schema::hasColumn($query->getModel()->getTable(), $column)) {
        $table = ($table_name) ? $table_name.'.' : $query->getModel()->getTable().'.';
        return $query->where($table.$column, '=', $company_id);
    }
}
```

**当用户 company_id = null 时**：
- Laravel 自动将 `where('assets.company_id', '=', null)` 转换为 `WHERE assets.company_id IS NULL`
- 查询结果：**所有 company_id 为 null 的资产**

---

## 2. 四类角色的完整权限模型（FMCS 开启）

### 2.1 角色定义

| 角色 | 判定条件 | user.company_id |
|------|---------|----------------|
| **超级管理员 (SuperUser)** | `$user->isSuperUser() === true` | 任意（通常为 null） |
| **无公司归属管理员** | `$user->hasAccess('admin') && $user->company_id === null` | null |
| **有公司归属管理员 (C1)** | `$user->hasAccess('admin') && $user->company_id === C1` | C1 |
| **普通用户 (C1)** | `!$user->hasAccess('admin') && !$user->isSuperUser()` | C1 |

---

### 2.2 各角色查询可见范围

#### 🎯 角色 1：超级管理员 (SuperUser)

```
查询发起
    │
    ▼
scopeCompanyables 检查
    ├─ FMCS 已启用? → 是
    ├─ 是 SuperUser? → 是 ✅
    └─ 跳过 Scope，返回原始查询
    │
    ▼
可见范围：✅ 所有公司 + 无公司归属的所有数据
```

**代码路径**：
- `Company.php:292` 条件满足 → 返回 `$query` 不修改
- 结果：所有数据

**写入权限**：
- `getIdForCurrentUser()` → `getIdFromInput()` → 返回用户输入值
- 可自由指定 company_id 为任意值（包括 null）

---

#### 🎯 角色 2：无公司归属管理员 (company_id = null)

```
查询发起
    │
    ▼
scopeCompanyables 检查
    ├─ FMCS 已启用? → 是
    ├─ 是 SuperUser? → 否
    ├─ 已登录? → 是
    └─ 进入 scopeCompanyablesDirectly
    │
    ▼
scopeCompanyablesDirectly
    ├─ $company_id = auth()->user()->company_id → null
    └─ 注入 WHERE company_id IS NULL （Laravel 自动转换）
    │
    ▼
可见范围：✅ 仅 company_id 为 null 的数据（无公司归属的数据）
```

**代码路径**：
- `Company.php:295` → 调用 `scopeCompanyablesDirectly()`
- `Company.php:325` + Laravel 自动转换 → `WHERE company_id IS NULL`
- 结果：仅无公司归属的数据

**策略层验证（isCurrentUserHasAccess）**：
```php
// app/Models/Company.php:191
return ($current_user_company_id == null)        // true ✅
    || ($current_user_company_id == $companyable_company_id) 
    || auth()->user()->isSuperUser();
```
- 第一个条件 `$current_user_company_id == null` 永远为 true
- 因此**策略层永远返回 true**，允许访问任何数据
- 但**查询层已过滤为仅无公司归属的数据**

**测试验证**（来自 `UpdateUserTest.php:373-395`）：
```php
// Admin without a company should allow updating user without a company ✅
// Admin without a company should get denied updating user from Company A ❌
// Admin without a company should get denied updating user from Company B ❌
```

**写入权限**：
- `getIdForCurrentUser()` → `$current_user->company_id == null` → 返回 `null`
- 新建/更新资产时，company_id 被强制设为 `null`
- ✅ 可以管理无公司归属的数据
- ❌ 无法访问或修改有公司归属的数据

---

#### 🎯 角色 3：有公司归属管理员 (company_id = C1)

```
查询发起
    │
    ▼
scopeCompanyables 检查
    ├─ FMCS 已启用? → 是
    ├─ 是 SuperUser? → 否
    ├─ 已登录? → 是
    └─ 进入 scopeCompanyablesDirectly
    │
    ▼
scopeCompanyablesDirectly
    ├─ $company_id = auth()->user()->company_id → C1
    └─ 注入 WHERE company_id = C1
    │
    ▼
可见范围：✅ 仅公司 C1 的数据
```

**代码路径**：
- `Company.php:295` → 调用 `scopeCompanyablesDirectly()`
- `Company.php:325` → `WHERE company_id = C1`
- 结果：仅公司 C1 的数据

**策略层验证**：
```php
return ($current_user_company_id == null)        // false
    || ($current_user_company_id == $companyable_company_id)  // C1 == 资产.company_id ✅/❌
    || auth()->user()->isSuperUser();           // false
```
- 同公司资产：✅ 允许访问
- 跨公司资产：❌ 拒绝访问
- 无公司归属资产：❌ 拒绝访问（C1 != null）

**测试验证**（来自 `UpdateUserTest.php:325-347`）：
```php
// Admin for Company A should allow updating user from Company A ✅
// Admin for Company A should get denied updating user from Company B ❌
// Admin for Company A should get denied updating user without a company ❌
```

**写入权限**：
- `getIdForCurrentUser()` → 返回 `$current_user->company_id` (C1)
- 新建/更新资产时，company_id 被强制设为 C1
- ❌ 无法跨公司移动资产
- ❌ 无法创建无公司归属的资产

---

#### 🎯 角色 4：普通用户 (company_id = C1)

```
查询发起
    │
    ▼
scopeCompanyables → 同角色 3 → WHERE company_id = C1
    │
    ▼
权限策略检查
    ├─ index: 需要 assets.view 权限
    ├─ view: 需要 assets.view 权限 + 公司匹配
    └─ 其他操作：通常无权限
    │
    ▼
可见范围：✅ 仅公司 C1 内被授权可见的数据
```

**代码路径**：
- 查询层：同角色 3，仅返回 C1 公司数据
- 策略层：除了公司检查，还需检查具体业务权限
- 结果：公司 C1 内被授权可见的数据

**写入权限**：
- 通常无 assets.edit 权限，无法修改资产
- 如有 self-service 权限，仅可查看自己的资产

---

### 2.3 角色权限对比总表

| 维度 | SuperUser | 无公司归属 Admin | 有公司归属 Admin (C1) | 普通用户 (C1) |
|------|-----------|-----------------|---------------------|-------------|
| **查询可见范围** | 所有公司 + 无公司 | 仅无公司归属的数据 | 仅公司 C1 | 仅公司 C1（限授权功能） |
| **查询 SQL 条件** | 无额外条件 | `WHERE company_id IS NULL` | `WHERE company_id = C1` | `WHERE company_id = C1` |
| **策略层返回** | 永远 true | 永远 true（但查询已过滤） | 同公司 true | 同公司 + 业务权限 |
| **写入 company_id** | 自由指定（含 null） | 强制 null | 强制 C1 | 无权限 |
| **能否管理无公司数据** | ✅ 是 | ✅ 是 | ❌ 否 | ❌ 否 |
| **能否管理公司 C1 数据** | ✅ 是 | ❌ 否 | ✅ 是 | 限授权功能 |
| **能否管理公司 C2 数据** | ✅ 是 | ❌ 否 | ❌ 否 | ❌ 否 |
| **跨公司移动资产** | ✅ 可以 | ❌ 不可以（只能设为 null） | ❌ 不可以 | ❌ 否 |

---

## 3. 写入操作的公司字段强制收敛

### 3.1 核心逻辑：getIdForCurrentUser

```php
// app/Models/Company.php:127-145
public static function getIdForCurrentUser($unescaped_input)
{
    if (! self::isFullMultipleCompanySupportEnabled()) {
        return self::getIdFromInput($unescaped_input);  // FMCS 关闭：原样返回
    }
    
    $current_user = auth()->user();
    
    if ($current_user->isSuperUser()) {
        return self::getIdFromInput($unescaped_input);  // SuperUser：原样返回
    }
    
    if ($current_user->company_id != null) {
        return $current_user->company_id;  // 有公司归属：强制使用自己的公司
    }
    
    return null;  // 无公司归属：强制设为 null
}
```

### 3.2 getIdFromInput 辅助逻辑

```php
// app/Models/Company.php:109-118
public static function getIdFromInput($unescaped_input)
{
    $escaped_input = e($unescaped_input);
    
    if ($escaped_input == '0') {
        return null;  // 输入 '0' 转换为 null
    }
    
    return $escaped_input;
}
```

### 3.3 写入收敛矩阵

| 角色 | 输入 company_id | 实际写入值 | 说明 |
|------|----------------|-----------|------|
| SuperUser | C2 | C2 | 自由指定 |
| SuperUser | 0 | null | `getIdFromInput('0')` 返回 null |
| SuperUser | null | null | 原样返回 |
| 无公司归属 Admin | C2 | null | 强制覆盖 |
| 无公司归属 Admin | 0 | null | 强制覆盖 |
| 无公司归属 Admin | null | null | 强制覆盖 |
| 有公司归属 Admin (C1) | C2 | C1 | 强制覆盖为自己的公司 |
| 有公司归属 Admin (C1) | 0 | C1 | 强制覆盖为自己的公司 |
| 有公司归属 Admin (C1) | null | C1 | 强制覆盖为自己的公司 |

### 3.4 写入路径分布

| 操作场景 | 代码位置 |
|---------|---------|
| 资产创建（API） | `app/Http/Controllers/Api/AssetsController.php:709` |
| 资产创建（Web） | `app/Http/Controllers/Assets/AssetsController.php:140` |
| 资产更新（API） | `app/Http/Controllers/Api/AssetsController.php:766-768` |
| 资产更新（Web） | `app/Http/Controllers/Assets/AssetsController.php:463` |
| 批量更新（Web） | `app/Http/Controllers/Assets/BulkAssetsController.php:374-379` |

---

## 4. 跨公司移动资产的字段一致性校验

### 4.1 校验场景分析

#### 场景 1：SuperUser 跨公司移动资产（API）

```
PUT /api/v1/hardware/123
{
    "company_id": "C2",
    "location_id": "L1"  // L1 原属于 C1
}
```

**校验流程**：
1. `getIdForCurrentUser('C2')` → 返回 `'C2'`（SuperUser 自由指定）
2. `UpdateAssetRequest` 验证规则：
   - `location_id` → `fmcs_location` 规则
   - 检查 `Location::find(L1)->company_id == 请求中的 company_id (C2)`
3. 如果 L1 属于 C1 ≠ C2 → ❌ 验证失败

**代码位置**：`app/Providers/ValidationServiceProvider.php:352-365`

#### 场景 2：SuperUser 批量跨公司移动（Web）

```
POST /hardware/bulkedit
{
    "ids": [123, 456],
    "company_id": "C2",
    "rtd_location_id": "L2",  // L2 属于 C2
    "update_real_loc": "1"
}
```

**校验流程**：
1. `getIdForCurrentUser('C2')` → 返回 `'C2'`
2. `$this->update_array['company_id'] = 'C2'`
3. `$this->update_array['location_id'] = 'L2'`
4. `$asset->update($this->update_array)`
   - 模型验证：`fmcs_location` 检查 L2 的 company_id == 资产的新 company_id (C2)
   - ✅ 验证通过

**潜在风险**：如果只更新 company_id 不更新 location_id，且 `update_real_loc` 未设置：
- 资产 company_id = C2
- 资产 location_id = L1（仍属于 C1）
- ❌ 数据不一致，但不会触发验证错误（因为验证规则检查的是请求数据，不是现有数据）

#### 场景 3：借出时的跨公司检查

```php
// app/Http/Controllers/Api/AssetsController.php:895-906
private function checkoutCompanyMismatchResponse(Asset $asset, $target): ?JsonResponse
{
    if (FMCS 启用
        && $asset->company_id !== null
        && $target->company_id !== null
        && $asset->company_id !== $target->company_id) {
        return error('general.error_user_company');
    }
    return null;
}
```

**检查点**：
- ✅ 借出目标与资产必须同公司
- ✅ 仅在借出时检查
- ❌ 资产已借出后变更公司不检查

---

### 4.2 字段一致性校验矩阵

| 操作 | 字段 | 校验时机 | 校验逻辑 | 代码位置 |
|------|------|---------|---------|---------|
| 创建/更新 | company_id | 写入前 | `getIdForCurrentUser` 强制转换 | `Company.php:127-145` |
| 创建/更新 | location_id | 验证时 | `fmcs_location` 检查位置公司 == 资产公司 | `ValidationServiceProvider.php:352-365` |
| 创建/更新 | rtd_location_id | 验证时 | `fmcs_location` 检查 | 同上 |
| 借出 | assigned_to | 借出时 | `checkoutCompanyMismatchResponse` 检查目标公司 == 资产公司 | `AssetsController.php:895-906` |
| 批量更新 | company_id | 写入前 | `getIdForCurrentUser` 强制转换 | `BulkAssetsController.php:374-379` |
| 批量更新 | location_id | 写入时 | 模型验证 `fmcs_location`（如果同时更新） | `BulkAssetsController.php:522` |
| 批量更新 | 已分配资产的 assigned_to | - | ❌ 无校验 | - |

---

## 5. 权限判定完整流程图

```
用户发起操作
    │
    ├─ 查询操作
    │   │
    │   ▼
    │   CompanyableScope 应用
    │   ├─ FMCS 启用?
    │   │   ├─ 否 → 返回所有数据
    │   │   └─ 是 → 检查是否 SuperUser
    │   │        ├─ 是 → 返回所有数据
    │   │        └─ 否 → 注入 WHERE company_id = {user.company_id}
    │   │                  （null 时自动转换为 IS NULL）
    │   │
    │   ▼
    │   策略层检查（SnipePermissionsPolicy::before）
    │   └─ Company::isCurrentUserHasAccess($item)
    │      ├─ SuperUser? → ✅ 放行
    │      ├─ user.company_id == null? → ✅ 放行（但查询已过滤）
    │      └─ user.company_id == item.company_id? → ✅/❌
    │
    └─ 写入操作
        │
        ▼
        company_id 处理（getIdForCurrentUser）
        ├─ FMCS 关闭? → 原样返回
        ├─ SuperUser? → 原样返回
        ├─ user.company_id != null? → 强制返回 user.company_id
        └─ user.company_id == null? → 强制返回 null
        │
        ▼
        模型验证
        ├─ location_id → fmcs_location 检查
        ├─ rtd_location_id → fmcs_location 检查
        └─ 其他字段验证
        │
        ▼
        借出时额外检查
        └─ checkoutCompanyMismatchResponse
```

---

## 6. 关键发现与修正总结

### 6.1 之前理解的修正点

| 之前理解 | 修正后结论 | 证据 |
|---------|-----------|------|
| 无公司归属管理员查询空结果 | 无公司归属管理员查询到 `company_id IS NULL` 的数据 | Laravel 自动将 `= null` 转换为 `IS NULL` |
| 无公司归属管理员可访问所有数据 | 无公司归属管理员仅可访问无公司归属的数据 | `UpdateUserTest.php:373-395` 测试用例 |
| 无公司归属管理员可跨公司移动 | 无公司归属管理员只能将资产设为 null | `getIdForCurrentUser()` 强制返回 null |

### 6.2 三类管理员的权限边界

| 管理员类型 | 可管理数据范围 | 公司字段写入权限 |
|-----------|--------------|----------------|
| **SuperUser** | 所有公司 + 无公司归属 | 自由指定任意值 |
| **无公司归属 Admin** | 仅无公司归属的数据 | 强制设为 null |
| **有公司归属 Admin (C1)** | 仅公司 C1 的数据 | 强制设为 C1 |

### 6.3 权限体系的设计特点

1. **三层防护**：查询 Scope → 策略检查 → 写入强制转换
2. **NULL 语义**：company_id = null 是一个独立的"权限域"，不是"无限制"
3. **SuperUser 特权**：唯一可以跨所有域操作的角色
4. **一致性保障**：fmcs_location 规则确保写入时位置与公司匹配

---

## 7. 代码引用速查

| 功能 | 文件 | 行号 |
|------|------|------|
| 查询 Scope 入口 | `app/Models/Company.php` | 289-297 |
| 查询 Scope 实际注入 | `app/Models/Company.php` | 305-328 |
| 写入公司 ID 处理 | `app/Models/Company.php` | 127-145 |
| 输入值转换 | `app/Models/Company.php` | 109-118 |
| 策略层公司访问检查 | `app/Models/Company.php` | 153-196 |
| 策略层前置拦截 | `app/Policies/SnipePermissionsPolicy.php` | 38-73 |
| 位置一致性验证规则 | `app/Providers/ValidationServiceProvider.php` | 352-365 |
| 借出公司一致性检查 | `app/Http/Controllers/Api/AssetsController.php` | 895-906 |
| 无公司归属管理员测试 | `tests/Feature/Users/Api/UpdateUserTest.php` | 373-395 |
| 有公司归属管理员测试 | `tests/Feature/Users/Api/UpdateUserTest.php` | 325-347 |

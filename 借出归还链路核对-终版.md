# Snipe-IT 借出归还链路核对文档（终版）

## 文档说明

本文档是对 Snipe-IT 资产借出归还链路的最终代码级核对结论，重点澄清：
1. 网页入口与 API 入口归还时间字段（`last_checkin`）的写入差异
2. 普通归还字段清空与设置的精确列表（消除所有矛盾表述）
3. 所有结论均附精确代码位置证据

---

## 核心结论速览

| 核对项 | 网页入口 (Web) | API 接口 |
|--------|---------------|---------|
| **last_checkin 写入** | ✅ 写入，`save()` 前设置，支持自定义日期 | ✅ 写入，方法入口立即设置为 `now()` |
| **last_checkin 设置时机** | 第 160 行，所有字段更新完成后 | 第 1115 行，方法入口最开始 |
| **last_checkin 是否支持自定义** | ✅ 支持 `checkin_at` 参数 | ❌ 始终为 `now()` |
| **强制清空字段** | `expected_checkin`、`accepted`、`assigned_to`、`assigned_type` | `expected_checkin`、`accepted`、`assigned_to`、`assigned_type` |
| **可更新字段** | `name`、`status_id`、`requestable`、`location_id`、`rtd_location_id`、`last_checkin` | `name`、`status_id`、`location_id`、`rtd_location_id`、`last_checkin` |
| **子资产级联** | ✅ 有 | ✅ 有（代码完全相同） |
| **级联清理** | ✅ 许可证 + 验收记录 | ✅ 许可证 + 验收记录（代码完全相同） |

---

## 1. 归还时间字段（last_checkin）精确核对

### 1.1 网页入口归还时间写入

**代码位置：** `app/Http/Controllers/Assets/AssetCheckinController.php:153-160`

```php
// 第 153-160 行
// Handle last checkin date
$checkin_at = date('Y-m-d H:i:s');                              // 默认：当前时间
if (($request->filled('checkin_at')) && ($request->input('checkin_at') != date('Y-m-d'))) {
    $originalValues['action_date'] = $checkin_at;
    $checkin_at = $request->input('checkin_at');                // 自定义：表单 checkin_at 参数
}
$asset->last_checkin = $checkin_at;                             // 写入数据库字段
```

**执行顺序：**
```
1. expected_checkin = null              (第 109 行)
2. disassociate()                       (第 110 行)
3. accepted = null                      (第 111 行)
4. name 更新                            (第 112 行)
5. status_id 更新                       (第 114-116 行)
6. requestable 更新                     (第 127-129 行)
7. location_id 重置为 rtd_location_id   (第 136 行)
8. location_id 表单覆盖                 (第 138-149 行)
9. originalValues 快照                  (第 151 行)
10. last_checkin = $checkin_at          (第 160 行)  ← 时间字段写入
11. 许可证清理                          (第 162-164 行)
12. 验收记录删除                        (第 167-174 行)
13. $asset->save()                      (第 181 行)
```

**关键特性：**
- ✅ 支持 `checkin_at` 表单参数自定义归还日期
- ✅ 写入时机在 `save()` 之前，所有业务字段更新完成之后
- ✅ 自定义日期会记录到 `originalValues['action_date']` 用于日志

### 1.2 API 入口归还时间写入

**代码位置：** `app/Http/Controllers/Api/AssetsController.php:1113-1115`

```php
// 第 1113-1115 行
$asset->expected_checkin = null;
// $asset->last_checkout = null;        ← 注释代码，实际不执行
$asset->last_checkin = now();          ← 立即设置为当前时间
$asset->assignedTo()->disassociate($asset);
$asset->accepted = null;
```

**执行顺序：**
```
1. expected_checkin = null              (第 1113 行)
2. last_checkin = now()                 (第 1115 行)  ← 时间字段写入（方法最开始）
3. disassociate()                       (第 1116 行)
4. accepted = null                      (第 1117 行)
5. name 更新 (has 判断)                 (第 1119-1121 行)
6. location_id 重置为 rtd_location_id   (第 1125 行)
7. location_id 表单覆盖                 (第 1127-1133 行)
8. status_id 更新                       (第 1135-1137 行)
9. checkin_at 变量计算 (用于事件)       (第 1139 行)
10. 许可证清理                          (第 1146-1148 行)
11. 验收记录删除                        (第 1151-1162 行)
12. $asset->save()                      (第 1164 行)
```

**关键特性：**
- ❌ 不支持自定义归还日期，始终为 `now()`（当前时间）
- ✅ 写入时机在方法最开始，所有其他字段更新之前
- ⚠️ 存在 `checkin_at` 请求参数但仅用于事件 `action_date`，不更新 `last_checkin` 字段

### 1.3 归还时间字段对比表

| 对比项 | 网页入口 | API 接口 |
|--------|---------|---------|
| **是否写入 last_checkin** | ✅ 是 | ✅ 是 |
| **写入时机** | 所有字段更新完成后，save() 之前 | 方法入口最开始 |
| **值来源** | `date('Y-m-d H:i:s')` 或 `request('checkin_at')` | `now()` |
| **支持自定义日期** | ✅ 是，通过 `checkin_at` 参数 | ❌ 否，始终为当前时间 |
| **代码行号** | `AssetCheckinController.php:160` | `Api\AssetsController.php:1115` |
| **last_checkout 是否清空** | ❌ 保留原值 | ❌ 保留原值（有注释代码但未启用） |

### 1.4 代码证据完整引用

**网页入口完整归还字段更新：**
```php
// app/Http/Controllers/Assets/AssetCheckinController.php:109-160
$asset->expected_checkin = null;           // 行 109
$asset->assignedTo()->disassociate($asset); // 行 110
$asset->accepted = null;                    // 行 111
$asset->name = $request->input('name');     // 行 112

// status_id 更新 (行 114-116)
// requestable 更新 (行 127-129)
// location_id 重置与覆盖 (行 136-149)

$asset->last_checkin = $checkin_at;         // 行 160 ← 归还时间写入
```

**API 入口完整归还字段更新：**
```php
// app/Http/Controllers/Api/AssetsController.php:1113-1137
$asset->expected_checkin = null;           // 行 1113
$asset->last_checkin = now();              // 行 1115 ← 归还时间写入
$asset->assignedTo()->disassociate($asset); // 行 1116
$asset->accepted = null;                    // 行 1117

// name 更新 (行 1119-1121)
// location_id 重置与覆盖 (行 1125-1133)
// status_id 更新 (行 1135-1137)
```

---

## 2. 普通归还字段变更清单（终版，无矛盾）

### 2.1 字段变更分类

| 分类 | 字段 | 网页入口 | API 接口 | 说明 |
|------|------|---------|---------|------|
| **🔴 强制清空** | `expected_checkin` | 设为 null | 设为 null | 预期归还日期 |
| **🔴 强制清空** | `assigned_to` | 设为 null | 设为 null | `disassociate()` 自动处理 |
| **🔴 强制清空** | `assigned_type` | 设为 null | 设为 null | `disassociate()` 自动处理 |
| **🔴 强制清空** | `accepted` | 设为 null | 设为 null | 验收状态 |
| **🟡 可更新** | `last_checkin` | 设置为 `$checkin_at` | 设置为 `now()` | 实际归还时间 |
| **🟡 可更新** | `name` | 无条件更新 | `has('name')` 时更新 | 资产名称 |
| **🟡 可更新** | `status_id` | 表单提供时更新 | 表单提供时更新 | 状态标签 |
| **🟡 可更新** | `requestable` | 可设为 true（联动） | ❌ 不处理 | 可申请性 |
| **🟡 可更新** | `location_id` | 重置为 `rtd_location_id`，可表单覆盖 | 重置为 `rtd_location_id`，可表单覆盖 | 当前位置 |
| **🟡 可更新** | `rtd_location_id` | `update_default_location == 0` 时更新 | `update_default_location == true` 时更新 | 默认位置 |
| **❌ 保留原值** | `last_checkout` | 不变 | 不变 | 上次借出时间 |
| **❌ 保留原值** | `checkout_counter` | 不变 | 不变 | 借出次数 |
| **❌ 保留原值** | `checkin_counter` | 不变 | 不变 | 归还次数（事件监听中更新） |
| **❌ 保留原值** | `asset_tag` | 不变 | 不变 | 资产编号 |
| **❌ 保留原值** | `model_id` | 不变 | 不变 | 型号 ID |
| **❌ 保留原值** | `serial` | 不变 | 不变 | 序列号 |
| **❌ 保留原值** | `purchase_date` | 不变 | 不变 | 购买日期 |
| **❌ 保留原值** | `purchase_cost` | 不变 | 不变 | 购买成本 |
| **❌ 保留原值** | `order_number` | 不变 | 不变 | 订单号 |
| **❌ 保留原值** | `warranty_months` | 不变 | 不变 | 保修月份 |
| **❌ 保留原值** | `notes` | 不变 | 不变 | 备注 |
| **❌ 保留原值** | `company_id` | 不变 | 不变 | 公司 ID |
| **❌ 保留原值** | `supplier_id` | 不变 | 不变 | 供应商 ID |
| **❌ 保留原值** | `byod` | 不变 | 不变 | 是否自带设备 |

### 2.2 字段操作执行顺序（网页入口）

```
app/Http/Controllers/Assets/AssetCheckinController.php

┌─ 行 109: expected_checkin = null                          🔴 清空
├─ 行 110: assignedTo()->disassociate($asset)               🔴 清空 assigned_to + assigned_type
├─ 行 111: accepted = null                                  🔴 清空
├─ 行 112: name = request('name')                           🟡 更新（无条件）
├─ 行 114-116: status_id = request('status_id')             🟡 更新（条件）
├─ 行 127-129: requestable = true                           🟡 更新（条件+联动）
├─ 行 136: location_id = rtd_location_id                    🟡 重置默认位置
├─ 行 138-149: location_id = request('location_id')         🟡 表单覆盖
├─ 行 142-144: rtd_location_id = request('location_id')     🟡 同步更新默认位置（条件）
├─ 行 151: originalValues = getRawOriginal()                快照
├─ 行 154-160: last_checkin = $checkin_at                   🟡 设置归还时间
├─ 行 162-164: licenseseats->each()->update()               🔄 级联：许可证释放
├─ 行 167-174: CheckoutAcceptance::pending()->delete()      🔄 级联：验收记录删除
├─ 行 181: $asset->save()                                   💾 持久化
├─ 行 183-185: Asset::where()->update()                     🔄 级联：子资产位置更新
└─ 行 187: event(new CheckoutableCheckedIn)                 📡 触发事件
```

### 2.3 字段操作执行顺序（API 入口）

```
app/Http/Controllers/Api/AssetsController.php

┌─ 行 1113: expected_checkin = null                         🔴 清空
├─ 行 1115: last_checkin = now()                            🟡 设置归还时间（入口立即设置）
├─ 行 1116: assignedTo()->disassociate($asset)              🔴 清空 assigned_to + assigned_type
├─ 行 1117: accepted = null                                 🔴 清空
├─ 行 1119-1121: name = request('name')                     🟡 更新（has 判断）
├─ 行 1125: location_id = rtd_location_id                   🟡 重置默认位置
├─ 行 1127-1133: location_id = request('location_id')       🟡 表单覆盖
├─ 行 1130-1132: rtd_location_id = request('location_id')   🟡 同步更新默认位置（条件）
├─ 行 1135-1137: status_id = request('status_id')           🟡 更新（条件）
├─ 行 1139: checkin_at = 日期计算 (仅用于事件)              📝 不更新字段
├─ 行 1140: originalValues = getRawOriginal()               快照
├─ 行 1146-1148: licenseseats->each()->update()             🔄 级联：许可证释放
├─ 行 1151-1162: CheckoutAcceptance::pending()->delete()    🔄 级联：验收记录删除
├─ 行 1164: $asset->save()                                  💾 持久化
├─ 行 1167-1169: Asset::where()->update()                   🔄 级联：子资产位置更新
└─ 行 1171: event(new CheckoutableCheckedIn)                📡 触发事件
```

### 2.4 关键字段行为澄清

**`disassociate()` 方法效果：**
```php
// Eloquent MorphTo::disassociate() 同时清空两个字段
$asset->assignedTo()->disassociate($asset);
// 等效于：
// $asset->assigned_to = null;
// $asset->assigned_type = null;
```

**`requestable` 字段差异：**
- 网页入口：`request()->boolean('set_requestable') && $isDeployableStatus` 时设为 `true`
- API 入口：无任何处理，保留原值

**`name` 字段更新差异：**
- 网页入口：无条件 `$asset->name = $request->input('name')`（可能设为空）
- API 入口：`$request->has('name')` 时才更新（保留原值除非显式指定）

**`update_default_location` 参数差异：**
- 网页入口：`$request->input('update_default_location') == 0` 时更新 `rtd_location_id`
- API 入口：`$request->input('update_default_location') == true` 时更新 `rtd_location_id`

---

## 3. 重复归还边界判定（证据闭环）

### 3.1 判定标准统一

**所有控制层使用完全相同的判定标准：**
```php
is_null($asset->assignedTo)
```

**`assignedTo()` 关联定义：**
```php
// app/Models/Asset.php:734-737
public function assignedTo()
{
    return $this->morphTo('assigned', 'assigned_type', 'assigned_to')->withTrashed();
}
```

### 3.2 四层防护机制代码证据

| 层级 | 代码位置 | 检测代码 |
|------|---------|---------|
| 网页表单入口 | `AssetCheckinController.php:37-41` | `if (is_null($asset->assignedTo))` |
| 网页提交 | `AssetCheckinController.php:92-94` | `if (is_null($target = $asset->assignedTo))` |
| API 提交 | `Api\AssetsController.php:1104-1111` | `if (is_null($target = $asset->assignedTo))` |
| 模型方法（未调用） | `Asset.php:488-500` | `availableForCheckIn()` 未被控制层调用 |

### 3.3 边界场景判定矩阵

| 场景 | `assigned_to` | `assigned_type` | 目标记录状态 | `assignedTo()` 返回 | 重复归还判定 |
|------|--------------|----------------|-------------|---------------------|-------------|
| 正常未借出 | null | null | - | null | ✅ 已归还 |
| 正常已借出 | 123 | `App\Models\User` | 存在 | User 对象 | ❌ 未归还 |
| 脏数据A | 123 | null | 存在 | null | ✅ 误判为已归还 |
| 脏数据B | null | `App\Models\User` | - | null | ✅ 已归还 |
| 孤立分配 | 123 | `App\Models\User` | 硬删除 | null | ✅ 误判为已归还 |
| 目标软删除 | 123 | `App\Models\User` | 软删除 | User 对象（withTrashed） | ❌ 未归还 |

### 3.4 孤立分配特殊处理

**检测方法：**
```php
// app/Models/Asset.php:2178-2182
public function hasOrphanedAssignment(): bool
{
    return ($this->assigned_to && ! $this->assigned_type)
        || ($this->assigned_to && $this->assigned_type && ! $this->assignedTo);
}
```

**触发条件（满足任一即可）：**
1. `assigned_to` 非空 且 `assigned_type` 为空 → 脏数据
2. `assigned_to` 非空 且 `assigned_type` 非空 但 `assignedTo` 为空 → 目标硬删除

---

## 4. 子资产级联与级联清理（代码完全一致）

### 4.1 子资产位置级联

**网页入口 & API 入口（代码完全相同）：**
```php
// AssetCheckinController.php:182-185 / Api\AssetsController.php:1166-1169
if ($asset->save()) {
    Asset::where('assigned_type', Asset::class)
        ->where('assigned_to', $asset->id)
        ->update(['location_id' => $asset->location_id]);
```

**筛选条件：**
- `assigned_type = 'App\Models\Asset'`（借出给资产）
- `assigned_to = 当前资产ID`

### 4.2 级联清理

**许可证座位释放（代码完全相同）：**
```php
// AssetCheckinController.php:162-164 / Api\AssetsController.php:1146-1148
$asset->licenseseats->each(function (LicenseSeat $seat) {
    $seat->update(['assigned_to' => null]);
});
```

**验收记录删除（代码完全相同）：**
```php
// AssetCheckinController.php:167-174 / Api\AssetsController.php:1151-1162
CheckoutAcceptance::pending()
    ->whereHasMorph('checkoutable', [Asset::class], function ($query) use ($asset) {
        $query->where('id', $asset->id);
    })
    ->get()
    ->map(function ($acceptance) {
        $acceptance->delete();
    });
```

---

## 5. 三类入口行为差异总表（终版）

### 5.1 归还流程差异

| 对比项 | 网页入口 (Web UI) | API 接口入口 |
|--------|------------------|-------------|
| **last_checkin 写入** | ✅ `save()` 前设置，支持自定义 | ✅ 方法入口立即设置，不支持自定义 |
| **last_checkin 值来源** | `date('Y-m-d H:i:s')` 或 `request('checkin_at')` | `now()` |
| **name 更新条件** | 无条件 | `$request->has('name')` |
| **requestable 联动** | `set_requestable` + `isDeployableStatus` 校验 | 无处理，保留原值 |
| **update_default_location** | `== 0` 时更新默认位置 | `== true` 时更新默认位置 |
| **重复归还检测** | `create()` + `store()` 双重检测 | `store()` 单次检测 |
| **子资产级联** | ✅ 有（代码同 API） | ✅ 有（代码同网页） |
| **级联清理** | ✅ 许可证 + 验收记录（代码同 API） | ✅ 许可证 + 验收记录（代码同网页） |
| **事务包裹** | 无 | 无 |
| **响应格式** | Redirect + Session flash | JSON `{status, payload, messages}` |

### 5.2 借出流程差异（补充）

| 对比项 | 网页入口 (Web UI) | API 接口入口 | 批量借出入口 |
|--------|------------------|-------------|-------------|
| **子资产级联（借出）** | ✅ 仅 location 类型有 | ❌ 无 | ❌ 无 |
| **子资产级联（归还）** | ✅ 全部类型有 | ✅ 全部类型有 | 不支持 |
| **事务包裹** | 无 | `DB::transaction()` | `DB::transaction()` |
| **requestable 处理** | `set_not_requestable` 复选框 | 保留原值除非显式指定 | `set_not_requestable` 复选框 |
| **事件触发** | `CheckoutableCheckedOut` | `CheckoutableCheckedOut` | `CheckoutablesCheckedOutInBulk` |

---

## 6. 最终结论（无矛盾版）

### 6.1 归还时间字段结论

✅ **网页入口和 API 入口都会写入 `last_checkin` 字段**
- 网页入口：在所有字段更新完成后、`save()` 之前写入，支持通过 `checkin_at` 参数自定义日期
- API 入口：在方法入口最开始立即写入，始终为 `now()`，不支持自定义日期

### 6.2 字段变更结论

**强制清空字段（4 个）：**
1. `expected_checkin` → null
2. `accepted` → null
3. `assigned_to` → null（`disassociate()` 自动处理）
4. `assigned_type` → null（`disassociate()` 自动处理）

**可更新字段（6 个）：**
1. `last_checkin` → 归还时间（网页支持自定义，API 不支持）
2. `name` → 资产名称（网页无条件，API 有条件）
3. `status_id` → 状态标签（表单提供时更新）
4. `requestable` → 可申请性（仅网页支持，联动 `deployable`）
5. `location_id` → 当前位置（重置为默认，可表单覆盖）
6. `rtd_location_id` → 默认位置（条件同步更新）

**保留原字段（N 个）：**
`last_checkout`、`checkout_counter`、`checkin_counter`、`asset_tag`、`model_id`、`serial`、`purchase_date`、`purchase_cost`、`order_number`、`warranty_months`、`notes`、`company_id`、`supplier_id`、`byod` 等所有未列出字段

### 6.3 重复归还边界结论

- **判定标准：** `is_null($asset->assignedTo)`（MorphTo 关联返回空）
- **生效防护：** 3 层（网页表单入口、网页提交、API 提交）
- **未调用方法：** 模型层 `availableForCheckIn()` 方法未被控制层使用
- **边界场景：** 脏数据和孤立分配会被误判为已归还，需通过 `hasOrphanedAssignment()` 检测后使用强制归还流程

---

## 文档版本历史

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-05-19 | 初稿，包含 API 归还有无子资产级联等初步核对 |
| v2.0 (终版) | 2026-05-19 | 澄清归还时间字段写入差异，消除字段变更矛盾表述，提供精确代码行号证据 |

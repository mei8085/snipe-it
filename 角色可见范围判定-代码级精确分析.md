# Snipe-IT 角色可见范围判定 - 代码级精确分析

> **说明**：本文档基于代码逐行核对，修正了之前对无公司归属管理员行为的理解偏差。

---

## 1. 核心代码路径速查

| 功能 | 文件 | 行号 |
|------|------|------|
| 查询 Scope 入口 | `app/Models/Company.php` | 289-297 |
| 查询 Scope 实际注入 | `app/Models/Company.php` | 305-328 |
| 写入公司 ID 处理 | `app/Models/Company.php` | 127-145 |
| 权限策略前置检查 | `app/Models/Company.php` | 153-196 |
| 策略层拦截 | `app/Policies/SnipePermissionsPolicy.php` | 38-73 |
| 借出公司一致性检查 | `app/Http/Controllers/Api/AssetsController.php` | 895-906 |

---

## 2. FMCS 开启时的查询过滤逻辑（精确对齐代码）

### 2.1 ScopeCompanyables 入口逻辑

```php
// app/Models/Company.php:289-297
public static function scopeCompanyables($query, $column = 'company_id', $table_name = null)
{
    // 跳过 Scope 的三个条件（OR 关系）
    if (! self::isFullMultipleCompanySupportEnabled()       // 条件1: FMCS 未启用
        || (Auth::hasUser() && auth()->user()->isSuperUser())  // 条件2: 已登录且是 SuperUser
        || (! Auth::hasUser())) {                              // 条件3: 未登录（CLI 场景）
        return $query;  // 不做任何过滤
    } else {
        return self::scopeCompanyablesDirectly($query, $column, $table_name);
    }
}
```

### 2.2 ScopeCompanyablesDirectly 实际过滤逻辑

```php
// app/Models/Company.php:305-328
private static function scopeCompanyablesDirectly($query, $column = 'company_id', $table_name = null)
{
    $company_id = null;
    if (Auth::hasUser()) {
        $company_id = auth()->user()->company_id;  // 获取当前用户的 company_id
    }

    // 对 companies 表自身的特殊处理
    if ($query->getModel()->getTable() == 'companies') {
        return $query->where('companies.id', '=', $company_id);
    }

    // 通用：注入 WHERE 条件
    if (Schema::hasColumn($query->getModel()->getTable(), $column)) {
        $table = ($table_name) ? $table_name.'.' : $query->getModel()->getTable().'.';
        return $query->where($table.$column, '=', $company_id);  // 关键: 使用 = 运算符
    }
}
```

### 2.3 ⚠️ 关键发现：SQL 语义问题

**重要修正**：当 `$company_id = null` 时，生成的 SQL 是：
```sql
WHERE assets.company_id = NULL
```

在 SQL 标准中，`NULL = NULL` 的求值结果是 **UNKNOWN**（不是 TRUE），因此**不会匹配任何行**。正确的写法应该是 `WHERE assets.company_id IS NULL`。

这意味着：**无公司归属的管理员（company_id = null）在 FMCS 开启时，通过全局 Scope 查询不到任何数据！**

---

## 3. 四类角色的完整可见范围判定

### 3.1 角色定义

| 角色 | 判定条件 | company_id |
|------|---------|-----------|
| **超级管理员 (SuperUser)** | `$user->isSuperUser()` === true | 任意（通常为 null） |
| **无公司归属管理员** | `$user->hasAccess('admin')` && `$user->company_id === null` | null |
| **有公司归属管理员** | `$user->hasAccess('admin')` && `$user->company_id !== null` | C1（具体公司ID） |
| **普通用户** | `!$user->hasAccess('admin')` && `!$user->isSuperUser()` | C1（具体公司ID）|

---

### 3.2 各角色查询过滤流程（FMCS 开启）

#### 🎯 角色 1：超级管理员 (SuperUser)

```
查询发起
    │
    ▼
scopeCompanyables 检查
    ├─ FMCS 已启用? → 是
    ├─ 是 SuperUser? → 是 ✅
    └─ 跳过 Scope，返回原始查询
    │
    ▼
结果：所有公司的所有数据
```

**代码路径**：
- `Company.php:292` 条件满足 → 返回 `$query` 不修改
- 查询结果：✅ 所有数据

**写入行为**：
- `getIdForCurrentUser()` → `getIdFromInput()` → 返回用户输入值
- 可自由指定 company_id 为任意值

---

#### 🎯 角色 2：无公司归属管理员 (company_id = null)

```
查询发起
    │
    ▼
scopeCompanyables 检查
    ├─ FMCS 已启用? → 是
    ├─ 是 SuperUser? → 否
    ├─ 已登录? → 是
    └─ 进入 scopeCompanyablesDirectly
    │
    ▼
scopeCompanyablesDirectly
    ├─ $company_id = auth()->user()->company_id → null
    └─ 注入 WHERE company_id = NULL
    │
    ▼
结果：SQL 中 NULL = NULL 为 UNKNOWN → ❌ 空结果集
```

**代码路径**：
- `Company.php:295` → 调用 `scopeCompanyablesDirectly()`
- `Company.php:325` → 生成 `WHERE company_id = null`
- 查询结果：❌ **无任何数据**

**权限策略检查（isCurrentUserHasAccess）**：
```php
// app/Models/Company.php:191
return ($current_user_company_id == null)        // true ✅
    || ($current_user_company_id == $companyable_company_id) 
    || auth()->user()->isSuperUser();
```
- 策略层返回 `true`，允许访问
- **但查询层已经返回空结果**

**不一致性**：查询层看不到数据，但策略层允许访问。如果用户直接通过 URL 访问 `/hardware/123`（该资产属于任意公司），策略层会放行。

**写入行为**：
- `getIdForCurrentUser()` → `$current_user->company_id == null` → 返回 `null`
- 新建/更新资产时，company_id 被强制设为 `null`

---

#### 🎯 角色 3：有公司归属管理员 (company_id = C1)

```
查询发起
    │
    ▼
scopeCompanyables 检查
    ├─ FMCS 已启用? → 是
    ├─ 是 SuperUser? → 否
    ├─ 已登录? → 是
    └─ 进入 scopeCompanyablesDirectly
    │
    ▼
scopeCompanyablesDirectly
    ├─ $company_id = auth()->user()->company_id → C1
    └─ 注入 WHERE company_id = C1
    │
    ▼
结果：✅ 仅公司 C1 的数据
```

**代码路径**：
- `Company.php:295` → 调用 `scopeCompanyablesDirectly()`
- `Company.php:325` → 生成 `WHERE company_id = C1`
- 查询结果：✅ 公司 C1 的所有数据

**权限策略检查**：
```php
return ($current_user_company_id == null)        // false
    || ($current_user_company_id == $companyable_company_id)  // C1 == 资产.company_id ✅/❌
    || auth()->user()->isSuperUser();           // false
```
- 同公司资产：✅ 允许访问
- 跨公司资产：❌ 拒绝访问

**写入行为**：
- `getIdForCurrentUser()` → 返回 `$current_user->company_id` (C1)
- 新建/更新资产时，company_id 被强制设为 C1
- ❌ 无法跨公司移动资产

---

#### 🎯 角色 4：普通用户 (company_id = C1)

```
查询发起
    │
    ▼
scopeCompanyables → 同角色 3 → WHERE company_id = C1
    │
    ▼
权限策略检查
    ├─ index: 需要 assets.view 权限
    ├─ view: 需要 assets.view 权限 + 公司匹配
    ├─ create: 需要 assets.create 权限（通常没有）
    ├─ update: 需要 assets.edit 权限（通常没有）
    └─ delete: 需要 assets.delete 权限（通常没有）
    │
    ▼
结果：✅ 仅公司 C1 内被授权的数据
```

**代码路径**：
- 查询层：同角色 3，仅返回 C1 公司数据
- 策略层：除了公司检查，还需检查具体业务权限
- 查询结果：✅ 公司 C1 内被授权可见的数据

**写入行为**：
- 通常无 assets.edit 权限，无法修改资产
- 如有 self-service 权限，仅可查看自己的资产

---

### 3.3 角色行为对比矩阵（FMCS 开启）

| 维度 | SuperUser | 无公司归属 Admin | 有公司归属 Admin (C1) | 普通用户 (C1) |
|------|-----------|-----------------|---------------------|-------------|
| **查询可见范围** | 所有公司 | ❌ 空结果集 | 仅 C1 公司 | 仅 C1 公司（且限授权功能） |
| **直接 URL 访问跨公司资产** | ✅ 允许 | ✅ 允许（策略层放行） | ❌ 拒绝 | ❌ 拒绝 |
| **写入 company_id** | 自由指定 | 强制 null | 强制 C1 | 无权限 |
| **跨公司移动资产** | ✅ 可以 | ✅ 可以（设为 null） | ❌ 不可以 | ❌ 不可以 |
| **查询层过滤** | 无 | `WHERE company_id = NULL` | `WHERE company_id = C1` | `WHERE company_id = C1` |
| **策略层检查** | 永远放行 | company_id = null → 放行 | 同公司放行 | 同公司 + 业务权限 |

---

## 4. 写入操作的公司字段强制收敛路径

### 4.1 核心逻辑：getIdForCurrentUser

```php
// app/Models/Company.php:127-145
public static function getIdForCurrentUser($unescaped_input)
{
    if (! self::isFullMultipleCompanySupportEnabled()) {
        return self::getIdFromInput($unescaped_input);  // FMCS 关闭：原样返回
    } else {
        $current_user = auth()->user();
        
        if ($current_user->isSuperUser()) {
            return self::getIdFromInput($unescaped_input);  // SuperUser：原样返回
        } else {
            if ($current_user->company_id != null) {
                return $current_user->company_id;  // 有公司归属：强制使用自己的公司
            } else {
                return null;  // 无公司归属：强制设为 null
            }
        }
    }
}
```

### 4.2 写入路径分析

#### 路径 1：资产创建（API/Web）
```
StoreAssetRequest::prepareForValidation()
    │
    ▼
Company::getIdForCurrentUser($request->company_id)
    │
    ├─ SuperUser → 返回输入值
    ├─ 无公司归属 Admin → 返回 null
    └─ 有公司归属 Admin → 返回自己的 company_id
    │
    ▼
写入数据库
```

**代码位置**：
- API: `app/Http/Controllers/Api/AssetsController.php:766-768`
- Web: `app/Http/Controllers/Assets/AssetsController.php:463`

#### 路径 2：资产更新（API）
```
AssetsController::update()
    │
    ├─ $request->has('company_id')?
    │   └─ 是 → Company::getIdForCurrentUser() 强制转换
    │
    └─ 位置字段通过 fmcs_location 验证规则
        └─ 验证 location.company_id == asset.company_id
```

#### 路径 3：资产更新（Web）
```
AssetsController::update()
    │
    └─ $asset->company_id = Company::getIdForCurrentUser($request->input('company_id'))
```

#### 路径 4：批量更新（Web）
```
BulkAssetsController::post()
    │
    └─ if ($request->filled('company_id')) {
           $this->update_array['company_id'] = Company::getIdForCurrentUser(...)
       }
```

### 4.3 写入收敛总结

| 角色 | 输入 company_id | 实际写入值 | 说明 |
|------|----------------|-----------|------|
| SuperUser | C2 | C2 | 自由指定 |
| SuperUser | 0 | null | `getIdFromInput('0')` 返回 null |
| 无公司归属 Admin | C2 | null | 强制覆盖 |
| 无公司归属 Admin | 0 | null | 强制覆盖 |
| 有公司归属 Admin (C1) | C2 | C1 | 强制覆盖为自己的公司 |
| 有公司归属 Admin (C1) | 0 | C1 | 强制覆盖为自己的公司 |

---

## 5. 跨公司移动资产时的字段一致性校验

### 5.1 校验场景分析

#### 场景 1：SuperUser 跨公司移动资产（API）

```
PUT /api/v1/hardware/123
{
    "company_id": "C2",
    "location_id": "L1"  // L1 原属于 C1
}
```

**校验流程**：
1. `getIdForCurrentUser('C2')` → 返回 `'C2'`（SuperUser 自由指定）
2. `UpdateAssetRequest` 验证规则：
   - `location_id` → `fmcs_location` 规则
   - 检查 `Location::find(L1)->company_id == 请求中的 company_id (C2)`
3. 如果 L1 属于 C1 ≠ C2 → ❌ 验证失败

**代码位置**：`app/Providers/ValidationServiceProvider.php:352-365`

#### 场景 2：SuperUser 批量跨公司移动（Web）

```
POST /hardware/bulkedit
{
    "ids": [123, 456],
    "company_id": "C2",
    "rtd_location_id": "L2",  // L2 属于 C2
    "update_real_loc": "1"
}
```

**校验流程**：
1. `getIdForCurrentUser('C2')` → 返回 `'C2'`
2. `$this->update_array['company_id'] = 'C2'`
3. `$this->update_array['location_id'] = 'L2'`
4. `$asset->update($this->update_array)`
   - 模型验证：`fmcs_location` 检查 L2 的 company_id == 资产的新 company_id (C2)
   - ✅ 验证通过

**潜在风险**：如果只更新 company_id 不更新 location_id，且 `update_real_loc` 未设置：
- 资产 company_id = C2
- 资产 location_id = L1（仍属于 C1）
- ❌ 数据不一致，但不会触发验证错误（因为验证规则检查的是请求数据，不是现有数据）

#### 场景 3：借出时的跨公司检查

```php
// app/Http/Controllers/Api/AssetsController.php:895-906
private function checkoutCompanyMismatchResponse(Asset $asset, $target): ?JsonResponse
{
    if (FMCS 启用
        && $asset->company_id !== null
        && $target->company_id !== null
        && $asset->company_id !== $target->company_id) {
        return error('general.error_user_company');
    }
    return null;
}
```

**检查点**：
- ✅ 借出目标与资产必须同公司
- ✅ 仅在借出时检查
- ❌ 资产已借出后变更公司不检查

---

### 5.2 字段一致性校验矩阵

| 操作 | 字段 | 校验时机 | 校验逻辑 | 代码位置 |
|------|------|---------|---------|---------|
| 创建/更新 | company_id | 写入前 | `getIdForCurrentUser` 强制转换 | `Company.php:127-145` |
| 创建/更新 | location_id | 验证时 | `fmcs_location` 检查位置公司 == 资产公司 | `ValidationServiceProvider.php:352-365` |
| 创建/更新 | rtd_location_id | 验证时 | `fmcs_location` 检查 | 同上 |
| 借出 | assigned_to | 借出时 | `checkoutCompanyMismatchResponse` 检查目标公司 == 资产公司 | `AssetsController.php:895-906` |
| 批量更新 | company_id | 写入前 | `getIdForCurrentUser` 强制转换 | `BulkAssetsController.php:374-379` |
| 批量更新 | location_id | 写入时 | 模型验证 `fmcs_location`（如果同时更新） | `BulkAssetsController.php:522` |
| 批量更新 | 已分配资产的 assigned_to | - | ❌ 无校验 | - |

---

## 6. 代码缺陷与不一致性

### 6.1 缺陷 1：无公司归属管理员的查询空结果

**问题**：
```php
// scopeCompanyablesDirectly 生成
WHERE assets.company_id = NULL
```
SQL 中 `NULL = NULL` 不匹配任何行，导致无公司归属管理员看不到任何资产。

**影响**：
- 无公司归属管理员在资产列表页是空的
- 但直接访问资产详情页可以看到（策略层放行）

**建议修复**：
```php
// scopeCompanyablesDirectly 中
if ($company_id === null) {
    return $query->whereNull($table.$column);
} else {
    return $query->where($table.$column, '=', $company_id);
}
```

### 6.2 缺陷 2：查询层与策略层不一致

**问题**：
- 查询层：无公司归属管理员看不到数据
- 策略层：无公司归属管理员可以访问任意公司的数据

**影响**：
- 出现"列表看不到，但直接输入URL能访问"的奇怪现象
- 可能导致安全隐患

### 6.3 缺陷 3：批量更新公司时不检查现有分配

**问题**：
- 资产已分配给用户 U1（属于 C1）
- 批量更新资产 company_id 为 C2
- 不检查 U1 是否属于 C2
- 结果：资产属于 C2，但分配给 C1 的用户

**建议**：在批量更新时增加检查：
```php
if (isset($this->update_array['company_id']) && $asset->assigned_to) {
    $target = $asset->assignedTo;
    if ($target && $target->company_id != $this->update_array['company_id']) {
        // 报错或自动解除分配
    }
}
```

---

## 7. 结论与建议

### 7.1 核心结论

1. **Company 是唯一的权限隔离边界**：Location 和 Department 不构成独立的权限边界。

2. **四类角色行为差异显著**：
   - SuperUser：完全自由，不受限制
   - 无公司归属 Admin：查询空结果，但写入设为 null，策略层放行（存在不一致）
   - 有公司归属 Admin：严格限定在自己公司内
   - 普通用户：同公司 + 业务权限限制

3. **写入强制收敛**：`getIdForCurrentUser` 确保非 SuperUser 无法突破公司边界。

4. **存在代码缺陷**：无公司归属管理员的查询行为与设计预期不符。

### 7.2 关键代码核对清单

| 预期行为 | 实际代码行为 | 是否一致 |
|---------|-------------|---------|
| SuperUser 可跨公司操作 | ✅ 是 | 一致 |
| 有公司归属 Admin 仅限本公司 | ✅ 是 | 一致 |
| 无公司归属 Admin 可看所有公司 | ❌ 查询空结果 | 不一致（BUG） |
| 位置必须与资产同公司 | ✅ 写入时验证 | 一致（仅写入时） |
| 借出目标必须与资产同公司 | ✅ 借出时验证 | 一致（仅借出时） |
| 批量更新时检查数据一致性 | ❌ 不检查 | 不一致 |

### 7.3 优化建议

1. **修复查询空结果 Bug**：在 `scopeCompanyablesDirectly` 中区分 null 和非 null 情况，使用 `whereNull()` 处理 null 值。

2. **统一查询层与策略层行为**：要么让无公司归属 Admin 能看到所有数据，要么在策略层也限制其访问。

3. **增强批量更新的一致性检查**：更新 company_id 时检查现有 location_id 和 assigned_to 的公司归属。

4. **补充测试用例**：覆盖无公司归属管理员的查询场景。

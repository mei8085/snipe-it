# 资产批量导入：字段映射与校验流程分析

## 一、整体流程概览

资产导入的完整链路分为 **4 个核心阶段**，每个阶段都有独立的校验逻辑和报错分支：

```
用户上传CSV
    ↓
[阶段1] 文件上传与初始校验 (ImportController@store)
    ├─ 校验文件MIME类型
    ├─ 字符编码检测与转码（UTF-8）
    ├─ 读取表头行 & 首行数据
    └─ 重复表头检测
    ↓
[阶段2] 列映射自动匹配 (Livewire/Importer@updatingTypeOfImport)
    ├─ 精确匹配：表头名 vs 系统字段显示名（大小写不敏感）
    ├─ 别名匹配：通过 $aliases_fields 模糊匹配
    └─ 未匹配列标记为 null（Do Not Import）
    ↓
[阶段3] 导入执行 (ItemImportRequest@import → Importer@import)
    ├─ 构建 fieldMap（翻转 CSV列→系统字段 的映射关系）
    ├─ 逐行遍历 CSV 数据
    ├─ findCsvMatch() 通过映射查找数据
    └─ 业务字段解析（分类/厂商/位置/状态等 lookup）
    ↓
[阶段4] 模型校验与存储 (AssetImporter@createAssetIfNotExists)
    ├─ sanitizeItemForStoring() 过滤非 fillable 字段
    ├─ Eloquent 模型 save() 触发 ValidatingTrait 规则校验
    └─ 成功则写入 DB，失败则收集错误
```

---

## 二、阶段1：文件上传与初始校验

**核心代码位置**：[ImportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php#L41-L187)

### 2.1 MIME 类型校验

```php
// 允许的文件类型白名单
[
    'application/vnd.ms-excel',
    'text/csv',
    'application/csv',
    'text/x-Algol68',       // CSV的奇葩MIME
    'text/plain',
    'text/comma-separated-values',
    'text/tsv',
]
```

**报错分支**：`File type must be CSV. Uploaded file is xxx` → HTTP 422

### 2.2 字符编码检测与转码

使用 `EncodingDetector` 自动检测编码，非 UTF-8 时用 `iconv()` 转码。

**报错分支**：
- 转码失败：`transliterate_failure`（编码不支持转码）→ HTTP 422
- 表头含 malformed 字符：`header_row_has_malformed_characters` → HTTP 422
- 数据行含 malformed 字符：`content_row_has_malformed_characters` → HTTP 422

### 2.3 重复表头检测

```php
// ImportController.php L119-L136
for ($i = 0; $i < count($import->header_row); $i++) {
    $header = $import->header_row[$i];
    if (in_array($header, $import->header_row)) {
        $found_at = array_search($header, $import->header_row);
        if ($i > $found_at) {
            array_push($duplicate_headers, 
                "Duplicate header '$header' detected, first at column: ".($found_at + 1).', repeats at column: '.($i + 1));
        }
    }
}
```

**报错分支**：重复表头拼接成消息 → HTTP 422

---

## 三、阶段2：列映射自动匹配

**核心代码位置**：[Importer.php (Livewire)](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Livewire/Importer.php#L152-L201)

### 3.1 系统字段定义（资产类）

在 `mount()` 方法中定义了 `$assets_fields` 数组，键为**系统字段标识**，值为**用户可见的显示名**（用于精确匹配）：

| 系统字段标识 | 显示名（中文翻译后） |
|---|---|
| `asset_tag` | 资产编号 |
| `asset_model` | 型号名称 |
| `category` | 分类 |
| `manufacturer` | 厂商 |
| `location` | 位置 |
| `status` | 状态 |
| `serial` | 序列号 |
| `purchase_cost` | 采购金额 |
| `purchase_date` | 采购日期 |
| `warranty_months` | 保修期(月) |
| `supplier` | 供应商 |
| `company` | 公司 |
| `full_name` / `email` / `username` | 领用人相关 |
| `item_name` | 资产名称 |
| ...（共约40+字段） | ... |

资产类还会**动态追加自定义字段**（CustomField），通过数据库查询获取。

### 3.2 列映射匹配算法（3级优先级）

```
遍历 CSV 每一个表头列：
  │
  ├─ [第1级] 精确匹配：strcasecmp(表头名, 系统字段显示名) === 0
  │     匹配成功 → 直接绑定，跳过后续
  │
  ├─ [第2级] 别名匹配：遍历 $aliases_fields 数组
  │     │
  │     └─ strcasecmp(表头名, 别名) === 0
  │           匹配成功 → 再验证该字段是否属于当前导入类型
  │                         ├─ 属于 → 绑定
  │                         └─ 不属于（如配件导入含Warranty列）→ 放弃
  │
  └─ [第3级] 无匹配 → 设为 null（用户需手动选择 "Do Not Import"）
```

### 3.3 别名映射表示例

定义在 `$aliases_fields` 中，部分关键别名：

| 系统字段 | 可识别的别名 |
|---|---|
| `asset_model` | "Model Name", "Model" |
| `serial` | "Serial Number", "Serial No.", "Product Key", "Key" |
| `warranty_months` | "Warranty", "Warranty Months" |
| `order_number` | "Order #", "Order No.", "Order Num", "Order" |
| `full_name` | "Full Name", "Fullname", "领用人" |
| `email` | "Email", "E-mail" |
| `qty` | "QTY", "Quantity" |
| `zip` | "Postal Code", "Post Code", "Zip Code" |

**关键代码**：[Livewire/Importer.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Livewire/Importer.php#L467-L646)

---

## 四、阶段3：字段映射与行数据处理

### 4.1 映射关系反转

**核心代码**：[ItemImportRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Requests/ItemImportRequest.php#L33-L71)

```php
// 用户提交的 column-mappings 格式：CSV列名 => 系统字段标识
// 例如：["Asset Tag" => "asset_tag", "Category" => "category", ...]

// 反转后格式（键全小写）：系统字段标识 => CSV列名
// 例如：["asset_tag" => "asset tag", "category" => "category", ...]
$fieldMappings = array_change_key_case(array_flip($import->field_map), CASE_LOWER);
```

反转后传给 Importer 的 `setFieldMappings()`，与 `$defaultFieldMap` 合并。

### 4.2 行数据查找：findCsvMatch()

**核心代码**：[Importer.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/Importer.php#L235-L247)

```php
public function findCsvMatch(array $array, $key, $default = null)
{
    $val = $default;
    // 步骤1：先通过 fieldMap 把"系统字段名"翻译成"CSV列名"
    $key = $this->lookupCustomKey($key);
    
    // 步骤2：CSV行数据的键已全部转小写，直接查找
    if (array_key_exists($key, $array)) {
        $val = Encoding::toUTF8(trim($array[$key]));
    }
    return $val;
}
```

**数据查找示例流程**：
```
代码需要 category_id
  → 先取 category 名称：findCsvMatch($row, 'category')
  → lookupCustomKey('category') 查 fieldMap → 返回 'category'（CSV列名）
  → 从 $row['category'] 取到值 "笔记本电脑"
  → createOrFetchCategory() 查/建分类 → 得到 category_id
```

### 4.3 关联字段 lookup（createOrFetch 系列）

所有关联字段（分类/厂商/位置/状态/供应商/公司/型号/用户）都遵循 **"查找优先，不存在则创建"** 的模式：

| 方法 | 关联表 | 默认值策略 |
|---|---|---|
| `createOrFetchCategory()` | categories | 空 → "Unnamed Category" |
| `createOrFetchManufacturer()` | manufacturers | 空 → "Unknown" |
| `createOrFetchSupplier()` | suppliers | 空 → "Unknown" |
| `createOrFetchLocation()` | locations | 空 → 返回 null，不创建 |
| `createOrFetchStatusLabel()` | statuslabels | 空 → 返回 null（资产导入有默认兜底） |
| `createOrFetchCompany()` | companies | 空 → 直接创建 |
| `createOrFetchAssetModel()` | asset_models | model_name 空 → 用 model_number；再空 → "Unknown" |
| `createOrFetchUser()` | users | 空 username/first_name → 返回 false，不创建 |

这些方法在 `ItemImporter::handle()` 中被统一调用：[ItemImporter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/ItemImporter.php#L23-L100)

---

## 五、阶段4：模型校验与存储

### 5.1 字段过滤：sanitizeItemForStoring()

**核心代码**：[ItemImporter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/ItemImporter.php#L135-L150)

```php
protected function sanitizeItemForStoring($model, $updating = false)
{
    $item = collect($this->item);
    // 只保留模型的 $fillable 中定义的字段，其余全部丢弃
    $item = $item->only($model->getFillable());
    
    // 如果是更新模式，还要丢弃空值（不覆盖已有数据为null）
    if ($updating) {
        $item = $item->reject(function ($value) {
            return empty($value);
        });
    }
    return $item->toArray();
}
```

**Asset 模型的 fillable 字段**（约26个）：
`asset_tag, name, model_id, status_id, serial, purchase_cost, purchase_date, warranty_months, supplier_id, location_id, rtd_location_id, company_id, notes, order_number, image, requestable, byod, last_checkout, expected_checkin, last_audit_date, next_audit_date, last_checkin, asset_eol_date, eol_explicit, assigned_to, assigned_type`

### 5.2 类型转换与格式化

在 `AssetImporter::handle()` 中对部分字段进行强制类型转换：

| 字段 | 转换方式 | 异常处理 |
|---|---|---|
| `warranty_months` | `intval(trim(...))` | 非数字 → 转为 0 |
| `requestable` | `fetchHumanBoolean()` → 0/1 | "yes"/"y"/"true" → 1，其余用 filter_var |
| `byod` | `fetchHumanBoolean()` → 0/1 | 同上 |
| `purchase_date` | `date('Y-m-d', strtotime(...))` | 无效日期 → 1970-01-01 或 false |
| `last_checkin/out` | `parseOrNullDate()` → Carbon 解析 | 解析失败 → 返回 null 并 log |
| `expected_checkin` | `parseOrNullDate()` → Y-m-d | 解析失败 → 返回 null |
| 自定义字段（加密） | `Crypt::encrypt()` | - |

**fetchHumanBoolean() 实现**：
```php
// Importer.php L522-L535
public function fetchHumanBoolean($value)
{
    $true = ['yes', 'y', 'true'];
    if (in_array(strtolower($value), $true)) {
        return 1;
    }
    return (int) filter_var($value, FILTER_VALIDATE_BOOLEAN);
}
```

### 5.3 模型 save() 校验（ValidatingTrait）

Asset 模型 `use ValidatingTrait`，在 `save()` 时自动触发校验规则。若校验失败：

```php
// AssetImporter.php L232
if ($success) {
    // ... 成功逻辑
}
$this->logError($asset, 'Asset "'.$this->item['name'].'"');
```

调用链路：
```
logError($asset, $field)
  → errorCallback($item, $field, $item->getErrors())
  → ItemImportRequest::errorCallback()
  → $this->errors[$item->name][$field] = $errorString
  → 最终通过 HTTP 500 返回给前端
```

---

## 六、缺列处理逻辑详解

"缺列"分为 **3 种场景**，各自走不同的分支：

### 场景A：CSV文件物理上缺少某列（表头就没有）

**处理逻辑**：`findCsvMatch()` 中 `array_key_exists` 返回 false → 使用 `$default`（通常为 `null`）

```
例：CSV 没有 "Purchase Cost" 列
  → findCsvMatch($row, 'purchase_cost') 
  → lookupCustomKey 返回 'purchase cost' 
  → array_key_exists('purchase cost', $row) = false
  → 返回 null
  → $this->item['purchase_cost'] = null
  → sanitize 后保留 null（创建模式）或被过滤（更新模式）
  → 若该字段在模型规则中是 nullable → 正常写入 null
```

**测试验证**：`when_columns_are_missing_in_import_file` 测试表明，缺少 `purchaseCost`/`purchaseDate`/`status` 列时导入依然成功，status 会用默认的 "Ready to Deploy"。

### 场景B：用户手动将某列映射设为 "Do Not Import"（field_map 中有 null）

**校验分支**：[ItemImportRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Requests/ItemImportRequest.php#L48-L58)

```php
if ($import->field_map) {
    foreach ($import->field_map as $field => $fieldValue) {
        if (is_null($fieldValue)) {
            $errorMessage = trans('validation.import_field_empty', ['fieldname' => $field]);
            $this->errorCallback($import, $field, [$field => [$errorMessage]]);
            return $this->errors;  // 直接中止，不再往下处理
        }
    }
}
```

> ⚠️ **注意**：这个校验非常严格——只要 field_map 中有**任何一个 null 值**，立即报错返回，整个导入流程中止。但实际 Livewire 前端在匹配时可能会把未识别的列设为 null，这时需要用户在界面上手动选择。

### 场景C：必填关联字段的值为空（如 model 为空字符串）

**处理逻辑**：`shouldUpdateField()` 返回 false，跳过 lookup → 最终模型校验失败。

```
例：model 列值为空
  → $asset_model_name = findCsvMatch(...) = ''
  → shouldUpdateField('') = false
  → createOrFetchAssetModel() 直接 return
  → $this->item['model_id'] = null
  → sanitize 后 model_id = null
  → 模型 save() 校验："The model id field is required."
  → 报错
```

**测试验证**：`when_required_columns_are_missing_in_import_file` 测试验证了此场景，会返回 `model_id` 必填和 `asset_tag` 长度两个错误。

---

## 七、类型不符处理逻辑详解

### 7.1 数值类型不符（warranty_months / purchase_cost）

```
例：warranty_months = "3 months"
  → intval(trim("3 months")) = 3  ✅ 正常提取数字
  
例：purchase_cost = "abc"
  → 直接存入，模型规则若为 numeric → 校验失败报错
  
例：warranty_months = ""
  → intval("") = 0 → 写入 0（非报错，可能是业务陷阱）
```

### 7.2 布尔类型不符（requestable / byod）

```
例：requestable = "是"
  → strtolower("是") 不在 ['yes','y','true']
  → filter_var("是", FILTER_VALIDATE_BOOLEAN) = false
  → (int)false = 0
  
例：requestable = "1" / "true" / "yes"
  → 正确识别为 1
  
例：requestable = ""
  → 转 0
```

### 7.3 日期类型不符（purchase_date / last_checkout 等）

**purchase_date（旧版转换）**：
```
例：purchase_date = "2022/10/10"
  → strtotime("2022/10/10") = 时间戳
  → date('Y-m-d', 时间戳) = "2022-10-10"  ✅
  
例：purchase_date = "invalid-date"
  → strtotime("invalid-date") = false
  → date('Y-m-d', false) = "1970-01-01"  ⚠️ 不会报错，而是静默变成1970年！
```

**last_checkout 等（Carbon 解析）**：
```
例：last_checkout = "invalid-date"
  → CarbonImmutable::parse() 抛出异常
  → catch 中 log "Unable to parse date: ..."
  → 返回 null  ✅ 安全降级
```

> ⚠️ **不一致风险**：`purchase_date` 用 `strtotime()`（非法值→1970），`last_checkout` 等用 `Carbon::parse()`（非法值→null），两种策略存在差异。

### 7.4 枚举/关联值不符（status 名称不存在）

```
例：status = "不存在的状态名"
  → createOrFetchStatusLabel() 查不到
  → 自动创建新状态（deployable=1, pending=0, archived=0）
  → 返回新 id  ✅ 不会报错，自动创建
```

类似的，category/manufacturer/supplier/company/location/model 都是**自动创建**策略，只有**用户（User）**有更严格的校验（username/first_name 缺了就返回 false，不创建用户）。

---

## 八、校验报错分支全景图

按**触发时机**和**错误来源**分类：

### 8.1 导入前校验（文件级，HTTP 422）

| 错误场景 | 错误信息示例 | 代码位置 |
|---|---|---|
| 文件非CSV | `File type must be CSV. Uploaded file is xxx` | [ImportController@store L52-L62](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php#L52-L62) |
| 字符编码无法转码 | `transliterate_failure` | [ImportController@store L81-L88](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php#L81-L88) |
| 表头含乱码 | `header_row_has_malformed_characters` | [ImportController@store L108-L116](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php#L108-L116) |
| 数据行含乱码 | `content_row_has_malformed_characters` | [ImportController@store L141-L149](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php#L141-L149) |
| 表头有重复列 | `Duplicate header 'xxx' detected...` | [ImportController@store L119-L136](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php#L119-L136) |

### 8.2 映射校验（提交时，HTTP 500 / import-errors）

| 错误场景 | 错误信息示例 | 代码位置 |
|---|---|---|
| 映射表中有 null | `validation.import_field_empty` | [ItemImportRequest@import L48-L58](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Requests/ItemImportRequest.php#L48-L58) |

### 8.3 业务逻辑校验（逐行处理，HTTP 500 / import-errors）

| 错误场景 | 错误信息示例 | 代码位置 |
|---|---|---|
| asset_tag 已存在（非更新模式） | `An asset with the asset tag xxx already exists...` | [AssetImporter@createAssetIfNotExists L92-L97](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/AssetImporter.php#L92-L97) |
| 关联模型 save 失败 | `logError($model, '模型名')` → 调用模型 `getErrors()` | 多处 `logError` 调用 |
| User 数据不足（缺 username/full_name/first_name） | Log 记录 + 返回 false（不中断整个导入） | [Importer@createOrFetchUser L353-L359](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/Importer.php#L353-L359) |

### 8.4 模型规则校验（save() 时，HTTP 500 / import-errors）

典型来自 Asset 模型的 `$rules`（通过 ValidatingTrait）：

| 字段 | 典型校验规则 | 错误信息示例 |
|---|---|---|
| `asset_tag` | required / min:1 | `The asset tag field must be at least 1 characters.` |
| `model_id` | required / exists | `The model id field is required.` |
| `status_id` | required / exists | （新建时有默认值，较少触发） |
| `purchase_cost` | numeric | `The purchase cost must be a number.` |
| `warranty_months` | integer | `The warranty months must be an integer.` |
| 自定义字段 | 按字段定义校验 | （依 CustomField 规则而定） |

**错误收集格式**：
```php
$this->errors[$item->name][$fieldDescription] = [
    $fieldName => [$validationMessage1, $validationMessage2, ...]
];

// 实际返回示例：
// [
//   "My Laptop" => [
//     "Asset \"My Laptop\"" => [
//       "asset_tag" => ["The asset tag field must be at least 1 characters."],
//       "model_id" => ["The model id field is required."]
//     ]
//   ]
// ]
```

---

## 九、关键代码文件索引

| 文件 | 职责 |
|---|---|
| [ImportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Controllers/Api/ImportController.php) | 文件上传API（阶段1校验）+ 触发导入执行 |
| [Importer.php (Livewire)](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Livewire/Importer.php) | 前端UI、自动列匹配算法、字段/别名定义 |
| [ItemImportRequest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Http/Requests/ItemImportRequest.php) | 导入入口、映射反转、错误回调收集器 |
| [Importer.php (抽象基类)](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/Importer.php) | CSV解析、findCsvMatch、用户/部门查找、日期/布尔解析 |
| [ItemImporter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/ItemImporter.php) | 通用字段处理、关联lookup（分类/厂商/位置/状态/供应商/型号）、sanitize过滤 |
| [AssetImporter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Importer/AssetImporter.php) | 资产特有逻辑：自定义字段处理、create/update分支、checkout/checkin、字段类型转换 |
| [Import.php (Model)](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Models/Import.php) | Import 记录实体，含 header_row、first_row、field_map 的 json cast |
| [Asset.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/app/Models/Asset.php) | 资产模型，含 fillable 字段列表、Eloquent 校验规则 |
| [ImportAssetsTest.php](file:///d:/fz/0601-1/solo-dogfeeding/code/66-snipe-it/tests/Feature/Importing/Api/ImportAssetsTest.php) | 功能测试，覆盖缺列/重复tag/必填缺失/格式转换等场景 |

---

## 十、导入流程时序图（简化版）

```
User                  ImportController        ItemImportRequest      Importer (abstract)    AssetImporter      Asset Model
 │                        │                       │                      │                    │                  │
 │── upload CSV ─────────>│                       │                      │                    │                  │
 │                        │── MIME/Encoding check │                      │                    │                  │
 │                        │── Duplicate header chk│                      │                    │                  │
 │<── 422 error ──────────│ (if fail)             │                      │                    │                  │
 │                        │                       │                      │                    │                  │
 │── 选择映射 & 提交 ────>│── process() ─────────>│                      │                    │                  │
 │                        │                       │── 校验 field_map    │                    │                  │
 │                        │                       │ (null立即返回报错)   │                    │                  │
 │                        │                       │                      │                    │                  │
 │                        │                       │── import() ────────>│                    │                  │
 │                        │                       │                      │── 读表头、自定义字段│                  │
 │                        │                       │                      │── DB transaction   │                  │
 │                        │                       │                      │── foreach rows:    │                  │
 │                        │                       │                      │   handle($row) ──>│── parent::handle  │
 │                        │                       │                      │                    │   (lookup关联)   │
 │                        │                       │                      │                    │── 自定义字段处理  │
 │                        │                       │                      │                    │── createAsset...  │
 │                        │                       │                      │                    │  ├─ find/update   │
 │                        │                       │                      │                    │  ├─ sanitize fill │
 │                        │                       │                      │                    │  ├─ 类型转换      │
 │                        │                       │                      │                    │  └─ save() ─────>│
 │                        │                       │                      │                    │      校验rules   │
 │                        │                       │<── errorCallback ───│<── logError ──────│<── fail/success │
 │                        │                       │                      │                    │                  │
 │<── 500 import-errors ──│<── return errors ─────│ (有错误时)           │                    │                  │
 │<── 200 + redirect_url ─│<── return success ────│ (无错误时)           │                    │                  │
```

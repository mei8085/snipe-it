# Snipe-IT 借出归还链路核对文档

## 核对背景

本文档针对三类入口（网页入口、API 接口入口、批量借出入口）的实际行为差异进行代码级核对，澄清三个关键问题：
1. API 归还是否对子资产进行位置级联
2. 普通归还字段变更的精确描述
3. 重复归还边界判定的证据闭环

---

## 核对结论速览

| 核对项 | 结论 |
|--------|------|
| API 归还子资产级联 | ✅ **是**，API 归还与网页归还一致，均执行子资产位置级联 |
| 普通归还清空字段 | `expected_checkin`、`accepted`、`assigned_to`、`assigned_type` |
| 普通归还设置字段 | `last_checkin`（API 独有）、`location_id`（重置为默认位置） |
| 重复归还判定标准 | `$asset->assignedTo === null`（多态关联返回空） |
| 重复归还防护层级 | 三层实际生效（网页表单入口、网页提交、API 提交），模型方法未被调用 |

---

## 1. API 归还子资产位置级联核对

### 1.1 代码证据

**API 归还控制器 `Api\AssetsController::checkin()`：**

```php
// app/Http/Controllers/Api/AssetsController.php:1164-1169
if ($asset->save()) {

    // Update the location of any child assets
    Asset::where('assigned_type', Asset::class)
        ->where('assigned_to', $asset->id)
        ->update(['location_id' => $asset->location_id]);

    event(new CheckoutableCheckedIn($asset, $target, auth()->user(), $request->input('note'), $checkin_at, $originalValues));
```

**网页归还控制器 `AssetCheckinController::store()`：**

```php
// app/Http/Controllers/Assets/AssetCheckinController.php:181-187
if ($asset->save()) {
    // Update the location of any child assets
    Asset::where('assigned_type', Asset::class)
        ->where('assigned_to', $asset->id)
        ->update(['location_id' => $asset->location_id]);

    event(new CheckoutableCheckedIn($asset, $target, auth()->user(), $request->input('note'), $checkin_at, $originalValues));
```

### 1.2 核对结论

✅ **API 归还与网页归还完全一致**，均在 `$asset->save()` 成功后执行子资产位置级联更新：

```php
Asset::where('assigned_type', Asset::class)
    ->where('assigned_to', $asset->id)
    ->update(['location_id' => $asset->location_id]);
```

**子资产级联逻辑：**
- 筛选条件：`assigned_type = 'App\Models\Asset'` 且 `assigned_to = 当前资产ID`
- 更新操作：将所有匹配子资产的 `location_id` 设置为父资产的新位置
- 生效时机：`$asset->save()` 成功后，事件触发前

### 1.3 借出时子资产级联差异

**网页借出（有子资产级联）：**
```php
// app/Http/Traits/CheckInOutTrait.php:40-43
case 'location':
    $asset->location_id = $target->id;
    Asset::where('assigned_type', 'App\Models\Asset')->where('assigned_to', $asset->id)
        ->update(['location_id' => $asset->location_id]);
    break;
```

**API 借出（无子资产级联）：**
```php
// app/Http/Controllers/Api/AssetsController.php:1022-1036
// 内联设置 location_id，无子资产级联代码
$asset->location_id = $target->location_id;
```

**批量借出（无子资产级联）：**
```php
// app/Http/Controllers/Assets/BulkAssetsController.php:723-732
$checkout_success = $asset->checkOut(..., null); // location 参数传 null
// checkOut() 内会尝试设置位置，但无子资产级联
// 后续手动覆盖位置（withoutEvents），仍无子资产级联
```

### 1.4 子资产级联行为汇总

| 操作 | 网页入口 | API 接口 | 批量入口 |
|------|---------|---------|---------|
| **借出** | ✅ 仅 location 类型有级联 | ❌ 无 | ❌ 无 |
| **归还** | ✅ 全部有级联 | ✅ 全部有级联 | 不支持 |

---

## 2. 普通归还字段变更核对

### 2.1 字段变更精确清单

**网页归还 `AssetCheckinController::store()`：**

```php
// app/Http/Controllers/Assets/AssetCheckinController.php:109-160
$asset->expected_checkin = null;           // 清空：预期归还日期
$asset->assignedTo()->disassociate($asset); // 清空：assigned_to + assigned_type
$asset->accepted = null;                    // 清空：验收状态
$asset->name = $request->input('name');     // 可更新：资产名称

if ($request->filled('status_id')) {
    $asset->status_id = e($request->input('status_id')); // 可更新：状态
}

if ($request->boolean('set_requestable') && $isDeployableStatus) {
    $asset->requestable = true;             // 可更新：可申请性（联动保护）
}

$asset->location_id = $asset->rtd_location_id; // 设置：重置为默认位置
// 如表单提供 location_id，可覆盖
// 如 update_default_location = 0，同步更新 rtd_location_id

$asset->last_checkin = $checkin_at;         // 设置：实际归还时间
```

**API 归还 `Api\AssetsController::checkin()`：**

```php
// app/Http/Controllers/Api/AssetsController.php:1113-1137
$asset->expected_checkin = null;           // 清空：预期归还日期
$asset->last_checkin = now();              // 设置：实际归还时间（立即）
$asset->assignedTo()->disassociate($asset); // 清空：assigned_to + assigned_type
$asset->accepted = null;                    // 清空：验收状态

if ($request->has('name')) {
    $asset->name = $request->input('name'); // 可更新：资产名称
}

$asset->location_id = $asset->rtd_location_id; // 设置：重置为默认位置
// 如表单提供 location_id，可覆盖
// 如 update_default_location = true，同步更新 rtd_location_id

if ($request->filled('status_id')) {
    $asset->status_id = $request->input('status_id'); // 可更新：状态（无联动保护）
}
```

### 2.2 `disassociate()` 方法效果

Eloquent `MorphTo::disassociate()` 方法同时清空两个字段：
- `assigned_to` → `null`
- `assigned_type` → `null`

```php
// 来自 Eloquent 框架源码：
public function disassociate($model)
{
    $this->parent->setAttribute($this->foreignKey, null);
    $this->parent->setAttribute($this->morphType, null);
    // ...
}
```

### 2.3 字段变更对比表

| 字段 | 网页归还 | API 归还 | 说明 |
|------|---------|---------|------|
| `expected_checkin` | 🔴 设为 null | 🔴 设为 null | 清空预期归还日期 |
| `assigned_to` | 🔴 设为 null | 🔴 设为 null | `disassociate()` 自动处理 |
| `assigned_type` | 🔴 设为 null | 🔴 设为 null | `disassociate()` 自动处理 |
| `accepted` | 🔴 设为 null | 🔴 设为 null | 清空验收状态 |
| `last_checkin` | 🟡 设为 `$checkin_at` | 🟡 设为 `now()` | API 立即设置，网页在 save 前设置 |
| `location_id` | 🟡 重置为 `rtd_location_id` | 🟡 重置为 `rtd_location_id` | 均可表单覆盖 |
| `name` | 🟡 可更新 | 🟡 可更新 | API 仅在 `has('name')` 时更新 |
| `status_id` | 🟡 可更新 | 🟡 可更新 | 均可表单覆盖 |
| `requestable` | 🟡 可设为 true（联动） | ❌ 不处理 | API 无联动保护 |
| `rtd_location_id` | 🟡 可同步更新 | 🟡 可同步更新 | 条件不同 |
| `last_checkout` | ❌ 不变 | ❌ 不变（注释代码） | 均保留原值 |
| `checkout_counter` | ❌ 不变 | ❌ 不变 | 借出时更新 |
| `checkin_counter` | ❌ 不变 | ❌ 不变 | `logCheckin()` 中更新 |

**符号说明：**
- 🔴 强制清空（null）
- 🟡 可设置/更新
- ❌ 不处理（保留原值）

### 2.4 归还时字段操作顺序

```
1. 清空操作（固定执行）
   ├─ expected_checkin = null
   ├─ accepted = null
   └─ disassociate() → assigned_to = null, assigned_type = null

2. 可更新字段（条件执行）
   ├─ name（如表单提供）
   ├─ status_id（如表单提供）
   └─ requestable（网页特有，需状态可部署）

3. 位置重置
   └─ location_id = rtd_location_id（可表单覆盖）

4. 归还时间设置
   ├─ 网页：last_checkin = $checkin_at（支持自定义日期）
   └─ API：last_checkin = now()

5. 保存前级联清理
   ├─ 许可证座位：assigned_to = null
   └─ 验收记录：删除 pending 记录

6. 保存成功后
   ├─ 子资产位置级联更新
   └─ 触发 CheckoutableCheckedIn 事件
```

---

## 3. 重复归还边界判定核对

### 3.1 四层防护机制代码证据

**第一层：网页归还表单入口**
```php
// app/Http/Controllers/Assets/AssetCheckinController.php:33-41
public function create(Asset $asset, $backto = null): View|RedirectResponse
{
    $this->authorize('checkin', $asset);
    
    if (is_null($asset->assignedTo)) {
        return redirect()->route('hardware.index')
            ->with('error', trans('admin/hardware/message.checkin.already_checked_in'));
    }
```

**第二层：网页归还提交**
```php
// app/Http/Controllers/Assets/AssetCheckinController.php:92-94
if (is_null($target = $asset->assignedTo)) {
    return redirect()->route('hardware.index')
        ->with('error', trans('admin/hardware/message.checkin.already_checked_in'));
}
```

**第三层：API 归还提交**
```php
// app/Http/Controllers/Api/AssetsController.php:1104-1111
$target = $asset->assignedTo;
if (is_null($target)) {
    return response()->json(Helper::formatStandardApiResponse('error', [
        'asset_tag' => e($asset->asset_tag),
        'model' => e($asset->model->name),
        'model_number' => e($asset->model->model_number),
    ], trans('admin/hardware/message.checkin.already_checked_in')));
}
```

**第四层：模型方法（未实际调用）**
```php
// app/Models/Asset.php:488-500
public function availableForCheckIn()
{
    if (($this->assigned_to != '') && ($this->status) && ($this->status->archived == '0')
        && ($this->status->deployable == '1')
    ) {
        return true;
    }
    return false;
}
```

### 3.2 判定标准澄清

**实际生效的判定标准：** `is_null($asset->assignedTo)`

`assignedTo()` 是 Eloquent MorphTo 关联：
```php
// app/Models/Asset.php:734-737
public function assignedTo()
{
    return $this->morphTo('assigned', 'assigned_type', 'assigned_to')->withTrashed();
}
```

**关联返回 null 的情况：**
1. `assigned_to` 为 null → 关联返回 null ✅
2. `assigned_type` 为 null 但 `assigned_to` 非空 → 关联类型不明确，返回 null ⚠️
3. `assigned_to` 和 `assigned_type` 都有值，但目标记录不存在 → 关联返回 null → **孤立分配场景**

### 3.3 边界场景判定矩阵

| 场景 | `assigned_to` | `assigned_type` | 目标记录存在 | `$asset->assignedTo` | 重复归还判定 |
|------|--------------|----------------|-------------|---------------------|-------------|
| 正常未借出 | null | null | - | null | ✅ 已归还 |
| 正常已借出 | 123 | `App\Models\User` | 是 | User 对象 | ❌ 未归还 |
| 脏数据A | 123 | null | 是 | null | ✅ 误判为已归还 |
| 脏数据B | null | `App\Models\User` | - | null | ✅ 已归还 |
| 孤立分配 | 123 | `App\Models\User` | 否（硬删除） | null | ✅ 误判为已归还 |
| 软删除目标 | 123 | `App\Models\User` | 是（软删除） | User 对象（withTrashed） | ❌ 未归还 |

### 3.4 孤立分配的特殊处理

当目标被硬删除时，`assignedTo` 返回 null，但 `assigned_to` 和 `assigned_type` 仍有值：

```php
// app/Models/Asset.php:2178-2182
public function hasOrphanedAssignment(): bool
{
    return ($this->assigned_to && ! $this->assigned_type)
        || ($this->assigned_to && $this->assigned_type && ! $this->assignedTo);
}
```

**强制归还流程触发条件：**
1. `assigned_to` 非空 且 `assigned_type` 为空 → 脏数据
2. `assigned_to` 非空 且 `assigned_type` 非空 但 `assignedTo` 为空 → 目标硬删除

### 3.5 重复归还防护有效性

| 场景 | 网页表单入口 | 网页提交 | API 提交 | 模型方法 |
|------|-------------|---------|---------|---------|
| 正常未借出 | ✅ 拦截 | ✅ 拦截 | ✅ 拦截 | ✅ 返回 false |
| 正常已借出 | ✅ 放行 | ✅ 放行 | ✅ 放行 | ✅ 返回 true |
| 脏数据（有 assigned_to 无 assigned_type） | ❌ 拦截（误判） | ❌ 拦截（误判） | ❌ 拦截（误判） | ❌ 返回 true |
| 孤立分配（目标硬删除） | ❌ 拦截（误判） | ❌ 拦截（误判） | ❌ 拦截（误判） | ❌ 返回 true |
| 目标软删除 | ✅ 放行 | ✅ 放行 | ✅ 放行 | ✅ 返回 true |

### 3.6 证据闭环

**错误消息定义：**
```php
// resources/lang/*/admin/hardware/message.php
'checkin' => [
    'already_checked_in' => '该资产已被归还',
    // ...
],
```

**所有控制层统一使用同一个错误消息键，说明判定标准一致：**
- 网页入口：`trans('admin/hardware/message.checkin.already_checked_in')`
- API 入口：`trans('admin/hardware/message.checkin.already_checked_in')`

**所有控制层使用同一个检测方法：**
- `is_null($asset->assignedTo)`

---

## 4. 三类入口行为差异总表

### 4.1 借出流程差异

| 对比项 | 网页入口 (Web UI) | API 接口入口 | 批量借出入口 |
|--------|------------------|-------------|-------------|
| **目标解析** | `CheckInOutTrait::determineCheckoutTarget()` | 内联 switch 解析 | `CheckInOutTrait::determineCheckoutTarget()` |
| **可借出性校验** | `availableForCheckout()` | `availableForCheckout()` | 1. `hasUndeployableStatus()` 预过滤<br>2. `pluck('assigned_to')->unique()->filter()` 批量校验 |
| **位置写入** | `updateAssetLocation()` + location 类型级联 | 内联设置 `location_id` | `checkOut()` 内设置 + 手动覆盖（withoutEvents） |
| **子资产级联（借出）** | ✅ 仅 location 类型 | ❌ 无 | ❌ 无 |
| **子资产级联（归还）** | ✅ 全部类型 | ✅ 全部类型 | 不支持 |
| **事件触发** | `CheckoutableCheckedOut` | `CheckoutableCheckedOut` | `CheckoutablesCheckedOutInBulk` |
| **事务包裹** | 无 | `DB::transaction()` | `DB::transaction()` |
| **requestable 处理** | `set_not_requestable` 复选框 | 保留原值除非显式指定 | `set_not_requestable` 复选框 |
| **位置更新重复** | ❌ 无 | ❌ 无 | ✅ `checkOut()` 内 + 手动覆盖（共两次） |

### 4.2 归还流程差异

| 对比项 | 网页入口 (Web UI) | API 接口入口 |
|--------|------------------|-------------|
| **重复归还检测** | `create()` + `store()` 双重检测 | `store()` 单次检测 |
| **last_checkin 设置时机** | save() 前，支持自定义日期 | 方法入口立即设置为 now() |
| **name 字段更新** | 无条件更新（可能为空） | 仅 `has('name')` 时更新 |
| **requestable 联动** | `set_requestable` + `isDeployableStatus` 校验 | 无联动保护 |
| **update_default_location** | `== 0` 时更新默认位置 | `== true` 时更新默认位置 |
| **子资产级联** | ✅ 有 | ✅ 有（代码完全相同） |
| **级联清理** | ✅ 许可证 + 验收记录 | ✅ 许可证 + 验收记录（代码完全相同） |
| **事务包裹** | 无 | 无 |

---

## 5. 关键发现与修正

### 5.1 原分析错误修正

| 原分析内容 | 修正后结论 | 证据位置 |
|-----------|-----------|---------|
| "API 归还无子资产级联" | ❌ 错误，API 归还**有**子资产级联 | `Api\AssetsController.php:1166-1169` |
| "API 归还设置 last_checkin" | ✅ 正确，且在方法入口立即设置 | `Api\AssetsController.php:1115` |
| "网页归还不设置 last_checkin" | ❌ 错误，网页归还也设置 last_checkin | `AssetCheckinController.php:160` |
| "disassociate 仅清空 assigned_to" | ❌ 错误，同时清空两个字段 | Eloquent MorphTo::disassociate() |
| "模型方法 availableForCheckIn 被调用" | ❌ 错误，控制层未调用该方法 | 控制层直接用 `is_null($assignedTo)` |

### 5.2 潜在问题确认

1. **脏数据误判**：`assigned_to` 有值但 `assigned_type` 为空时，会被误判为已归还
2. **孤立分配误判**：目标被硬删除时，会被误判为已归还（需使用强制归还流程）
3. **字段不一致 Bug**：API 删除已借出资产时仅清空 `assigned_to` 未清空 `assigned_type`（`Api\AssetsController:930-932`）

---

## 6. 核对总结

### 6.1 三类入口核心一致性

1. **重复归还判定标准一致**：均使用 `is_null($asset->assignedTo)`
2. **归还字段清空一致**：均清空 `expected_checkin`、`accepted`、`assigned_to`、`assigned_type`
3. **子资产级联一致**：归还时均执行子资产位置级联更新
4. **级联清理一致**：均释放许可证座位、删除待处理验收记录

### 6.2 三类入口关键差异

1. **借出时子资产级联**：仅网页借出的 location 类型有级联，API 和批量借出均无
2. **requestable 联动**：仅网页归还有 `deployable` 校验，API 归还无
3. **位置更新重复**：批量借出存在重复设置位置的问题（`checkOut()` 内 + 手动覆盖）
4. **last_checkin 设置时机**：API 入口立即设置，网页入口在 save 前设置（支持自定义日期）

### 6.3 判定边界澄清

- **重复归还边界**：`$asset->assignedTo === null`（多态关联返回空）
- **孤立分配边界**：`hasOrphanedAssignment() === true`（字段有值但关联返回空）
- **脏数据边界**：`assigned_to` 与 `assigned_type` 不同时为空/非空（违反双向依赖约束）

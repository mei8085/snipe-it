# Snipe-IT 组织层级权限与资产可见性分析

## 1. 概述

Snipe-IT 通过三层组织架构（公司 Company、地点 Location、部门 Department）与权限策略、查询过滤机制相结合，实现了精细化的资产可见性与操作权限控制。核心功能围绕 **FMCS（Full Multiple Company Support，完整多公司支持）** 开关展开，形成一套完整的数据隔离体系。

---

## 2. 组织层级数据模型

### 2.1 核心模型关系

```
Company (1) ────> (N) User
Company (1) ────> (N) Asset
Company (1) ────> (N) Location
Company (1) ────> (N) Department
Location (1) ───> (N) User
Location (1) ───> (N) Department
Department (1) ─> (N) User
```

### 2.2 各模型关键字段

| 模型 | 字段 | 说明 |
|------|------|------|
| **Company** | `id`, `name`, `phone`, `fax`, `email`, `tag_color` | 顶级组织单元 |
| **Location** | `id`, `name`, `parent_id`, `company_id`, `manager_id`, `address*`, `ldap_ou` | 支持层级结构（`parent_id` 自引用），可选关联公司 |
| **Department** | `id`, `name`, `company_id`, `location_id`, `manager_id` | 部门关联公司和地点 |
| **User** | `id`, `company_id`, `location_id`, `department_id`, `manager_id`, `permissions` | 用户的三个维度组织归属 |
| **Asset** | `id`, `company_id`, `location_id`, `rtd_location_id`, `assigned_to`, `assigned_type` | 资产的公司、实际位置、默认位置、分配目标 |

### 2.3 CompanyableTrait 核心标记

所有需要公司级隔离的模型都使用 `CompanyableTrait`：

```php
// app/Models/Traits/CompanyableTrait.php
trait CompanyableTrait {
    public static function bootCompanyableTrait() {
        if (__CLASS__ != 'App\Models\Location') {
            static::addGlobalScope(new CompanyableScope);
        } else {
            if (Setting::getSettings()?->scope_locations_fmcs == 1) {
                static::addGlobalScope(new CompanyableScope);
            }
        }
    }
}
```

**关键设计**：
- Location 的公司范围是可选的（受 `scope_locations_fmcs` 配置控制）
- 其他模型（Asset、User、Department 等）自动应用 CompanyableScope
- 这为后续的查询过滤奠定了基础

---

## 3. 权限策略体系 (Policy Layer)

### 3.1 策略继承结构

```
SnipePermissionsPolicy (抽象基类)
    ├── CheckoutablePermissionsPolicy (可借出物品基类)
    │   ├── AssetPolicy
    │   ├── AccessoryPolicy
    │   ├── ComponentPolicy
    │   └── ConsumablePolicy
    ├── CompanyPolicy
    ├── LocationPolicy
    ├── DepartmentPolicy
    ├── UserPolicy
    └── ...
```

### 3.2 核心权限检查流程 (SnipePermissionsPolicy::before)

`app/Policies/SnipePermissionsPolicy.php:38-73` 定义了权限检查的前置拦截逻辑：

```php
public function before(User $user, $ability, $item)
{
    // 1. Admin 全局放行（但仍受 FMCS 公司范围约束）
    if ($user->hasAccess('admin')) {
        return true;
    }

    // 2. 如果不是具体模型实例，跳过公司检查（交给后续方法处理）
    if (! $item instanceof Model) {
        return;
    }

    // 3. FMCS 公司访问权限检查
    if (! Company::isCurrentUserHasAccess($item)) {
        return false;  // 跨公司访问直接拒绝
    }
}
```

### 3.3 公司访问判断逻辑 (Company::isCurrentUserHasAccess)

`app/Models/Company.php:153-196`

```php
public static function isCurrentUserHasAccess($companyable)
{
    // FMCS 未启用时，所有人都能访问
    if (! self::isFullMultipleCompanySupportEnabled()) {
        return true;
    }

    // 检查模型表是否有 company_id 字段
    if (! Schema::hasColumn($companyable->getModel()->getTable(), 'company_id')) {
        return true;
    }

    // 核心判断逻辑
    $current_user_company_id = auth()->user()->company_id;
    $companyable_company_id = $companyable->company_id;

    return 
        ($current_user_company_id == null) ||        // 用户无公司归属 → 可访问所有
        ($current_user_company_id == $companyable_company_id) ||  // 同公司
        auth()->user()->isSuperUser();             // 超级用户
}
```

**权限矩阵**：

| 用户类型 | FMCS 启用 | 可见资产范围 |
|---------|----------|------------|
| SuperUser | 是/否 | 所有公司资产 |
| Admin (无 company_id) | 是 | 所有公司资产 |
| Admin (有 company_id=C1) | 是 | 仅 C1 公司资产 |
| 普通用户 (有 company_id=C1) | 是 | 仅 C1 公司资产 |
| 任何用户 | 否 | 所有资产（无隔离） |

---

## 4. 查询过滤机制 (Query Scoping)

### 4.1 全局查询 Scope (CompanyableScope)

`app/Models/CompanyableScope.php:23-26`

```php
public function apply(Builder $builder, Model $model)
{
    return Company::scopeCompanyables($builder);
}
```

所有使用 `CompanyableTrait` 的模型在查询时自动应用此 Scope。

### 4.2 ScopeCompanyables 过滤逻辑

`app/Models/Company.php:289-297`

```php
public static function scopeCompanyables($query, $column = 'company_id', $table_name = null)
{
    // 跳过条件：FMCS未启用 OR 是超级用户 OR 未登录（CLI）
    if (! self::isFullMultipleCompanySupportEnabled() || 
        (Auth::hasUser() && auth()->user()->isSuperUser()) || 
        (! Auth::hasUser())) {
        return $query;
    }

    // 应用公司过滤
    return self::scopeCompanyablesDirectly($query, $column, $table_name);
}
```

### 4.3 实际查询注入 (ScopeCompanyablesDirectly)

`app/Models/Company.php:305-328`

```php
private static function scopeCompanyablesDirectly($query, $column = 'company_id', $table_name = null)
{
    $company_id = auth()->user()->company_id;

    // 针对 companies 表自身的特殊处理
    if ($query->getModel()->getTable() == 'companies') {
        return $query->where('companies.id', '=', $company_id);
    }

    // 通用：检查列是否存在，然后注入 WHERE 条件
    if (Schema::hasColumn($query->getModel()->getTable(), $column)) {
        $table = ($table_name) ? $table_name.'.' : $query->getModel()->getTable().'.';
        return $query->where($table.$column, '=', $company_id);
    }
}
```

### 4.4 查询过滤机制的应用范围

| 模型 | 自动应用 Scope | 说明 |
|------|--------------|------|
| Asset | 是 | 资产查询自动过滤为当前用户公司 |
| User | 是 | 用户查询自动过滤为当前用户公司 |
| Company | 是 | 公司列表仅显示当前用户公司 |
| Location | 可选 | 受 `scope_locations_fmcs` 配置控制 |
| Department | 是 | 部门查询自动过滤 |
| License/Accessory/Consumable/Component | 是 | 所有资产品类 |

**注意**：全局 Scope 意味着即使是 `Asset::all()` 也会被自动过滤，这是数据隔离的关键保障。

---

## 5. 资产操作中的组织层级约束

### 5.1 资产创建时的公司归属

`app/Http/Requests/StoreAssetRequest.php:33-45`

```php
public function prepareForValidation(): void
{
    $idForCurrentUser = is_numeric($this->company_id)
        ? Company::getIdForCurrentUser($this->company_id)
        : $this->company_id;

    $this->merge([
        'company_id' => $idForCurrentUser,
    ]);
}
```

`app/Models/Company.php:127-145`

```php
public static function getIdForCurrentUser($unescaped_input)
{
    if (! self::isFullMultipleCompanySupportEnabled()) {
        return self::getIdFromInput($unescaped_input);
    }
    
    $current_user = auth()->user();
    
    if ($current_user->isSuperUser()) {
        return self::getIdFromInput($unescaped_input);  // 超级用户可自由指定
    } else {
        if ($current_user->company_id != null) {
            return $current_user->company_id;  // 非超级用户强制使用自己的公司
        } else {
            return null;
        }
    }
}
```

### 5.2 资产位置的一致性验证 (fmcs_location 规则)

`app/Providers/ValidationServiceProvider.php:352-365`

```php
Validator::extend('fmcs_location', function ($attribute, $value, $parameters, $validator) {
    $settings = Setting::getSettings();
    // 仅当 FMCS 和位置范围都启用时才验证
    if ($settings->full_multiple_companies_support == '1' && $settings->scope_locations_fmcs == '1') {
        $company_id = array_get($validator->getData(), 'company_id');
        $location = Location::find($value);

        // 位置的公司必须与资产的公司一致
        if (($location) && ($company_id != $location->company_id)) {
            return false;
        }
    }
    return true;
});
```

Asset 模型中应用此规则的字段：
- `location_id` (`app/Models/Asset.php:123`)
- `rtd_location_id` (`app/Models/Asset.php:124`)
- `assigned_location` (`app/Models/Asset.php:138`)

### 5.3 部门的跨公司约束

`app/Models/Department.php:47` 定义的验证规则：
```php
'name' => 'required|max:255|is_unique_across_company_and_location:departments,name'
```

确保部门名称在同一公司和地点下唯一。

---

## 6. 跨公司移动资产的权限边界分析

### 6.1 批量更新时的公司变更

`app/Http/Controllers/Assets/BulkAssetsController.php:374-379`

```php
if ($request->filled('company_id')) {
    $this->update_array['company_id'] = Company::getIdForCurrentUser($request->input('company_id'));
    if ($request->input('company_id') == 'clear') {
        $this->update_array['company_id'] = null;
    }
}
```

**权限边界**：

| 角色 | 能否跨公司移动资产 | 说明 |
|------|------------------|------|
| SuperUser | 是 | `getIdForCurrentUser` 返回输入值，可自由指定 |
| Admin (无 company_id) | 是 | 同上 |
| Admin (有 company_id=C1) | 否 | 强制返回 C1，无法变更 |
| 普通用户 | 否 | 无 assets.edit 权限 |

### 6.2 跨公司移动的连锁影响

当资产的 `company_id` 变更时，需要验证以下一致性：

1. **位置一致性**：如果启用了 `scope_locations_fmcs`，新位置必须属于新公司
2. **分配目标一致性**：如果资产已分配给用户/位置/资产，分配目标也必须属于新公司
3. **关联资源一致性**：资产关联的许可证、配件、组件等也需要考虑公司边界

**潜在风险**：当前代码在批量更新公司时，仅更新了 `company_id` 字段，但未检查位置、分配目标等是否仍属于新公司。这可能导致数据不一致。

### 6.3 资产借出时的跨公司检查

资产 checkout 时（`app/Models/Asset.php:521-573`），虽然没有显式的跨公司检查，但：

1. 全局 Scope 确保用户只能看到自己公司的资产
2. 全局 Scope 确保用户只能选择自己公司的借出目标（User/Location/Asset）
3. 因此实际上无法跨公司借出资产

### 6.4 可见性与操作权限的关系

```
┌─────────────────────────────────────────────────────────┐
│          全局查询 Scope (CompanyableScope)              │
│  自动注入 WHERE company_id = X，用户看不到其他公司资产   │
└────────────────────────────┬────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────┐
│           权限策略 (SnipePermissionsPolicy)             │
│  - before() 检查公司访问权限                             │
│  - view/create/edit/delete 检查具体权限                  │
└────────────────────────────┬────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────┐
│           数据验证 (Validation Rules)                   │
│  - fmcs_location 确保位置与公司一致                      │
│  - getIdForCurrentUser 确保公司归属正确                   │
└─────────────────────────────────────────────────────────┘
```

---

## 7. 关键配置开关

| 配置项 | 位置 | 说明 |
|-------|------|------|
| `full_multiple_companies_support` | settings 表 | 启用/禁用完整多公司支持 |
| `scope_locations_fmcs` | settings 表 | 是否对 Location 也应用公司范围 |

**两开关组合效果**：

| FMCS | scope_locations | 效果 |
|------|-----------------|------|
| 0 | X | 无任何公司隔离 |
| 1 | 0 | 资产/用户/部门等隔离，地点不隔离 |
| 1 | 1 | 所有模型都按公司隔离，且位置必须匹配资产公司 |

---

## 8. 代码缺陷与优化建议

### 8.1 潜在问题：批量更新公司时的级联验证

**问题**：`BulkAssetsController::update()` 在更新 `company_id` 时，没有同步验证位置（`location_id`、`rtd_location_id`）的公司归属是否与新公司一致。

**风险**：可能出现资产属于公司 A，但位置属于公司 B 的不一致状态。

**优化建议**：
```php
if ($request->filled('company_id')) {
    $new_company_id = Company::getIdForCurrentUser($request->input('company_id'));
    $this->update_array['company_id'] = $new_company_id;
    
    // 验证位置一致性
    if ($settings->scope_locations_fmcs == '1') {
        if ($asset->location_id) {
            $location = Location::find($asset->location_id);
            if ($location && $location->company_id != $new_company_id) {
                // 报错或清除位置
            }
        }
    }
}
```

### 8.2 潜在问题：分配目标的跨公司检查

**问题**：资产 checkout 时，如果手动构造请求，可能绕过可见性检查分配给其他公司的用户。

**建议**：在 `Asset::checkOut()` 方法中增加显式的跨公司检查。

### 8.3 设计优点

1. **全局 Scope 设计**：通过 Eloquent 全局 Scope 实现隐式数据隔离，避免了在每个查询处手动编写过滤条件
2. **策略前置拦截**：`before()` 方法统一处理公司权限检查，策略类只需关注业务权限
3. **可配置性**：通过两个开关实现不同粒度的隔离需求
4. **非侵入式**：`CompanyableTrait` 可以轻松应用于任何需要公司隔离的模型

---

## 9. 总结

Snipe-IT 的组织层级权限体系采用了**三层防护**：

1. **查询层**：通过 `CompanyableScope` 全局 Scope 自动过滤查询结果，确保用户从根本上看不到其他公司的数据
2. **策略层**：通过 `SnipePermissionsPolicy::before()` 在权限检查之初就拦截跨公司访问
3. **验证层**：通过 `fmcs_location` 等自定义验证规则确保数据写入时的一致性

跨公司移动资产仅对超级用户和无公司归属的管理员开放，且在批量更新场景下存在一定的数据一致性风险。整体设计在安全性和灵活性之间取得了较好的平衡。
